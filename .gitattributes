# ⚠⚠ THIS FILE EXISTS TO PROTECT THE AUTHORED-SHA256 CONTRACT FROM GIT ITSELF.
#
# `core.autocrlf=true` on this machine. On CHECKOUT git rewrites LF -> CRLF for anything it
# considers text — so an artifact committed byte-for-byte would come back out of a fresh clone
# with different bytes, and its authored `sha256` would no longer match.
#
# ⚠ The failure would be silent and it would look like tampering: the hash mismatch is exactly the
# signal the contract uses to say "the channel altered this", and git would be producing it on a
# file nothing had touched. **A guard that cannot distinguish corruption from its own toolchain is
# not a guard.**
#
# Measured 2026-08-19: no `.gitattributes` existed, so nothing protected any file from the
# conversion. The artifacts below are delivered as FILES, hashed by their author over a declared
# range, and committed unmodified — see `docs/SPEC-2026-08-19-landed-artifact-provenance.md`.
#
# `-text` disables all end-of-line conversion in both directions. It is deliberately scoped to the
# hash-verified artifacts rather than applied to `docs/**`: a blanket rule would change how ~100
# existing documents round-trip, which is a different decision and not this one.

docs/D-093-amendment-2-paste-ready.md            -text
docs/ORDERS-Code-step-4-schema-ingest-surface.md -text
docs/ORDERS-Code-clinical-surface-today.md       -text
docs/ORDERS-Code-cancer-surface-attribution.md   -text

docs/ORDERS-Code-scoring-measurements.md   -text
docs/HPA-licence-2026-08-19-as-read.md     -text
docs/ORDERS-Code-ingest-through-the-gate.md -text
docs/ORDERS-Code-backup-readonly-phase1-F049.md -text
docs/F-047-amendment-1-paste-ready.md -text
docs/ORDERS-Code-rekey-rental-and-FD1.md -text
docs/PRICING-2026-08-22-all-remaining-tranches.md -text
docs/PREWORK-2026-08-23-the-fold.md -text
docs/BOOT-Planner-2026-08-22.md -text
docs/ORDERS-Code-2026-08-23-the-rental-run.md -text
docs/CLOSEOUT-2026-08-22.md -text

# ── 2026-08-20 — the same defect, on a DATA artifact, caught by the ingest's own guard ──
#
# ⚠⚠ `F-047` member 15 recurred. The rule above was scoped to `docs/` and nobody widened it when a
# hash-pinned artifact landed under `data/`. `census_features.v1.jsonl` was committed LF (the blob
# is intact and matches its manifest), checked out CRLF, and its sha256 on disk became
# `0a17cab2…` against a pinned `c08f9f1d…`. **Nothing had touched the file.**
#
# ⚠ It was found because `census_ingest_features.py` verifies its source before writing — the
# first thing the ingest does refused the ingest. That is the guard working, and it is also the
# measure of how the earlier one was scoped: a rule applied to one directory and not another is
# not a rule.
#
# ⚠ The pins below are the manifest's, not a comment's — re-derive, never trust this line.
data/census/census_features.v1.jsonl          -text
data/census/census_features.v1.manifest.json  -text
data/census/cohort_feature_baseline.json      -text

docs/CLOSEOUT-2026-08-19.md                                -text
docs/PREWORK-2026-08-20.md                                 -text
docs/ORDERS-Code-census-feature-extraction-overnight.md    -text
docs/F-051-paste-ready.md                                  -text
docs/D-079-amendment-structural-profile-paste-ready.md     -text
docs/ORDERS-Code-census-out-of-distribution.md             -text

# ⚠ And three more the FIRST measurement missed. My initial scan required the sha256 to sit on the
# same line as the marker; these three wrap it, so a narrower regex under-reported the class. The
# test derives the set from the tree instead, and found them. ⚠⚠ The lesson is the one this file
# already records one level up: the fix was scoped to what the detector happened to see.
# ⚠ CORRECTION, 2026-08-20: of the three below, only the AMENDMENT actually declares a hash (in a
# COLON form the detector first missed). The other two state in their own text that "no
# AUTHORED-SHA256 IS DECLARED, AND THAT IS DELIBERATE" — I added them on a wrong reading of a
# too-broad regex. They stay: `-text` on an unpinned document costs nothing, and removing them
# would erase the evidence of the misreading. ⚠⚠ Both errors are the same class as the defect this
# file exists to prevent — a detector scoped to what its author could see. See `F-052`.
docs/AMENDMENT-2026-08-19-planner-log-entries.md       -text
docs/ORDERS-Code-2026-08-19-clinical-edges-1-and-2.md  -text
docs/SPEC-2026-08-19-supplier-survey-clinical-edges.md -text

# ⚠ The alias index is DERIVED from the pinned UniProt cache and is compared against it by
# `tests/test_protein_aliases.py`. CRLF on checkout would not break that test, but it would
# change the file's bytes under any future pin — the same class as the members above, added
# when the file was created rather than after a hash mismatch taught us to.
data/census/protein_aliases.v1.csv -text

# ⚠ The second-instrument artifact (D-103), derived from a pinned proteinatlas.tsv and compared
# against it by tests. Protected at creation rather than after a hash mismatch taught us to.
data/census/surface_confirmation.v1.csv -text

# ⚠ The three documents received 2026-08-20, two of which declare an AUTHORED-SHA256 over a
# line-start-anchored range. F-047 member 15 recurring: CRLF on checkout changes the bytes and the
# hash stops matching a file nothing touched. Added at landing, not after a mismatch.
docs/D-075-amendment-1-paste-ready.md              -text
docs/ORDERS-Code-invariant-amendment-references.md -text
docs/D-093-amendment-2-paste-ready.md              -text

# ⚠ The 2026-08-20 reissue and its order, both declaring an AUTHORED-SHA256 over a line-start
# anchored range. Protected at landing (F-047 member 15).
docs/D-093-amendment-3-REISSUE.md                        -text
docs/ORDERS-Code-licence-quotation-and-attribution-audit.md -text

# ⚠ The deferral entry, AUTHORED-SHA256 over first #### -> EOF. Protected at landing.
docs/D-093-amendment-7-paste-ready.md -text

docs/ORDERS-Code-attribute-every-surface.md -text
docs/D-093-amendment-8-paste-ready.md      -text
data/census/hpa_ensg_map.csv               -text

docs/D-093-amendment-8-paste-ready.md -text
docs/D-093-amendment-9-paste-ready.md -text
docs/D-095-amendment-2-paste-ready.md -text
docs/CORRECTION-2026-08-20-F-043-amendment-numbering.md -text

docs/F-052-amendment-1-paste-ready.md -text

docs/D-079-amendment-6-paste-ready.md -text

# ⚠ Both sides of a merge added protection here and BOTH are kept. All four declare an
# AUTHORED-SHA256 over a line-start-anchored range, so dropping either side would leave a pinned
# file exposed to CRLF rewriting — F-047 member 15, which is what this whole file exists for.
docs/F-053-paste-ready.md -text
docs/ORDERS-Code-PAE-disagreement-and-timing.md -text
docs/SPEC-2026-08-20-backbone-graphic.md -text
docs/PROPOSAL-2026-08-20-F-042-remediation-and-P-006.md -text

docs/F-052-amendment-2-and-F-053-amendment-1.md -text
docs/CLOSEOUT-2026-08-20.md -text
docs/PREWORK-2026-08-21.md -text
docs/BOOT-Planner-2026-08-21.md -text
docs/ORDERS-Code-default-sort-and-the-3dmol-debt.md -text
docs/D-102-amendment-N-paste-ready.md -text
docs/F-054-and-F-047-amendment-paste-ready.md -text
docs/ORDERS-Code-viewer-defect-REISSUE.md -text
docs/BATCH-2026-08-21-four-entries.md -text
docs/ORDERS-Code-tumour-crosswalk-REISSUE.md -text
docs/ORDERS-Code-WE1-skin-confirmation-and-SEER-decision-6.md -text
docs/ORDERS-Code-antibody-provenance-and-pooling.md -text
docs/DRAFT-D-093-amendment-N-pooling-surface-contract-LANDED-d1466e00.md -text
docs/DRAFT-D-093-amendment-N-pooling-surface-contract-SUPERSEDED-f9da826f.md -text
docs/P-001-amendment-N-pooled-comparator.md -text
docs/DRAFT-D-093-amendment-N-pooling-surface-contract-SUPERSEDED-02d7856a.md -text
docs/ORDERS-Code-directional-or-dilution.md -text
docs/P-001-amendment-2-underpowered.md -text

# -- 2026-09-15 -- THE SAME DEFECT, A THIRD TIME, AT TWELVE TIMES THE SCALE (F-047 amendment 5) --
#
# !! The 2026-08-20 note above says of the 2026-08-19 rule: "a rule applied to one directory and
# not another is not a rule". It then widened the rule to THREE files under data/census and
# stopped. Twelve other hash-pinned artifacts were already in the tree, or landed later, and none
# was covered.
#
# ! Measured 2026-09-15 over every tracked file under data/: 168 are checked out CRLF, and of
# those TWELVE carry a pinned sha256 that matches only their LF bytes. The blobs are intact --
# `git cat-file -p :data/burden/seer_us_cancer_burden.v1.csv` hashes to the pinned ed4ad609...
# and contains no CRLF at all. Nothing has touched these files. Git rewrites them on checkout
# and the pin then fails on disk.
#
# !! THE COST WAS NOT A FAILING TEST, IT WAS A FAILING TEST NOBODY COULD READ. The local suite
# stood at ~34 red on Windows while CI was green, so the 35th failure arrives looking like the
# other 34. A baseline nobody can tell has drifted is not a baseline.
#
# ! Scope is still NOT a blanket rule -- the 2026-08-19 note declines that for docs/** and that
# reasoning stands. This covers exactly the artifacts a pinned sha256 is asserted over, and
# tests/test_pinned_artifacts_are_eol_protected.py enumerates them from the tree and fails if one
# is unlisted, so a fourth recurrence cannot be silent.
data/burden/seer_us_cancer_burden.v1.csv   -text
data/census/census_manifest.csv            -text
data/census/census_manifest.v2.csv         -text
data/census/census_manifest.v3.csv         -text
data/census/census_manifest.v4.csv         -text
data/census/census_manifest.v5.csv         -text
data/census/census_manifest.v6.csv         -text
data/census/census_manifest.v7.csv         -text
data/census/spans_annex.v2.csv             -text
data/census/spans_surface.v2.csv           -text
data/census/tranche6_runs.csv              -text
data/census/table_S3_surfaceome.xlsx.LFS-POINTER-NOT-THE-TABLE -text

# -- and the same defect on byte-pinned SOURCE modules, which is where the other 19 red were --
#
# !! Stopping at data/ would have been the 2026-08-20 mistake a third time. Measured the same way:
# fourteen tracked source files carry a sha256 pinned in tests/, and ALL FOURTEEN were drifting.
#
# ! The symptom is a wrong-but-plausible failure message, which is why it survived a month:
#   test_d130_residual_rmsd.py said "core/hold48_kabsch.py was edited" while `git status` said
#   clean and the index blob hashed to exactly the pinned value. Nothing edited it. The test was
#   hashing CRLF bytes against an LF pin, and reporting that as an edit.
#
# ! These pins exist to prove a PR did NOT touch a sibling module. A guard that cries "edited" on
# every clean checkout is a guard people learn to ignore -- which is the same failure mode as the
# standing 34-red baseline, one layer down.
app/census_structural_read.py                    -text
app/linker_seam_path_read.py                     -text
app/phase5_named_refuse.py                       -text
core/census_structural.py                        -text
core/hold48_confidence_kabsch.py                 -text
core/hold48_kabsch.py                            -text
core/hold48_linker_seam.py                       -text
core/hold48_piecewise_kabsch.py                  -text
core/hold48_stitch.py                            -text
core/scorer.py                                   -text
db/migrations/versions/0012_census_structural_rank.py -text
scripts/census_structural_rank.py                -text
scripts/tranche6_runs.py                         -text
ui/src/components/MethodNote.jsx                 -text

# -- 2026-09-15 -- D-167's evidence, protected AT LANDING rather than after a mismatch (ORDERS A8.5)
#
# !! Each of these is verified by an authored sha256 that a reader is expected to re-derive:
#   state_before.json  b825065d... the Phase B read, quoted in D-167 and in the sitting report
#   capture.json       88820b82... printed by the machine, checked by --capture-sha before the write
#   phase_e_read.json  4d8b4886... printed by the Phase E read
# A Windows checkout under core.autocrlf would rewrite them and their pins would fail on disk while
# the blobs stayed intact - F-047 member 15, the defect this file exists for, on the evidence that
# a production write rests on.
#
# ! The directory glob covers the sitting's console logs too, so a log quoted by hash later cannot
# drift. The three hash-pinned files are ALSO listed by name: the byte-for-byte guard in
# tests/test_pinned_artifacts_are_eol_protected.py enumerates paths from this file, and a glob would
# be skipped as "not present" rather than checked.
data/control/d167/**                   -text
data/control/d167/capture.json         -text
data/control/d167/state_before.json    -text
data/control/d167/phase_e_read.json    -text
