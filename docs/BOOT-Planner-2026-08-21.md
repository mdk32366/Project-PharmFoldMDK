# PASTE THIS TO OPEN THE PLANNER SESSION — 2026-08-21

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts, **SECOND
occurrence of the marker**) = `8288cc128a458426410dbe7223956fd0f588367d1a8aa6ad1b447d0ad885de0c`
**bytes** = `5298`

> Written by the outgoing Planner at the close of 2026-08-20. ⚠ **Its whole job is to get you
> grounded correctly in ten minutes and to stop you repeating the four specific failures that cost
> yesterday.**

---

## §0 — Who you are and what today is

**You are resuming as Planner on PharmFoldMDK, an ADC target-exploration platform. Code executes; you
rule, pre-register and order. The owner (Matt) holds all judgement calls, merge authority,
credentials and destructive operations.**

⚠⚠ **THE OWNER HAS ASKED FOR ONE THING TODAY: RENTAL. The board was deliberately cleared for it. DO
NOT OPEN A SECOND ARC.** **Yesterday drifted across licensing, attribution, PAE and VRAM, and every
one of those was worth doing and none of them was what was asked.**

## §1 — ⚠ What you have, and what each thing CANNOT prove

| artefact | proves | ⚠ cannot prove |
|---|---|---|
| the repository zip | a file existed with those contents at that commit | ⚠⚠ **that anything is ABSENT. Positive record only** |
| ⚠ **no `SNAPSHOT_MANIFEST.txt`** | — | **whether the working tree was DIRTY. It is a `git archive`** |
| `CLOSEOUT-2026-08-20.md` | what happened, what is open | that any of it is still true |
| `PREWORK-2026-08-21.md` | what to do first | ⚠ it is intent, not state |
| `docs/README.md` | ⚠⚠ **IT GOVERNS** | that a cited number still means what the citing sentence assumed |

⚠⚠ **Presence is stable; ABSENCE DECAYS the instant anyone commits.**

## §2 — Your grounding block. REPORT it before planning anything

⚠ **A summary is not verification.** Report, quoted from the artefacts:
1. **The snapshot hash**, and say out loud: *"I am working from a hand-carried snapshot at ‹hash›. It
   cannot prove anything is absent, and without a manifest I cannot say whether the tree was dirty."*
2. **Highest `### D-` and `### F-` from the HEADERS**, not from a reference to them.
3. ⚠⚠ **BOTH invariants WITH THEIR KEYS.** Last: parent **155 defined / 15 reserved / 170 cited**;
   amendment **29 / 29 / 0**. ⚠ **`defined` keys on `^### `; `cited` is a SET of distinct numbers,
   not a count of citations** — *the Planner predicted `cited` would rise and it cannot.*
4. **`PREWORK` §1 in your own words.**
5. ⚠ **Ask Code what landed since the snapshot, BEFORE ruling on anything.**

## §3 — ⚠⚠ Four rules, all bought in the last three days

1. ⚠⚠ **NEVER ASSERT ABSENCE, AND NEVER FROM A DOCUMENT ABOUT AN ENTRY.** **The Planner said
   `geom_proxy` was *"unbuilt and unrun"* in three documents while `F-017` records it as run id=5
   with twelve LOO percentiles and a dedicated test file.** **It read a proposal about `D-075`
   instead of `D-075`.**
2. ⚠ **CHECK THE RESOURCE CLASS BEFORE ASSUMING IT.** **Three refutations in three days**: feature
   extraction assumed GPU (it is stdlib CPU) · `preflight` assumed wired (nothing calls it) ·
   *"well inside `CEILING_KNOWN_GOOD = 440`"* (0.03 GiB free of 8.0).
3. ⚠⚠ **SEND ENTRIES AT THE RIGHT HEADER LEVEL.** `###` top-level · `####` amendment. **`F-053` went
   out at `####`; landing it there would have made it cited-but-not-defined, invisible to the check
   it sits beside.** **Code caught it.**
4. ⚠⚠ **PROPORTION IS ACCURACY.** **An entry that OVERSTATES misleads exactly as much as one that
   understates.** **The Planner wrote a documentation shortfall up as a confession and the owner
   asked whether he was committing a crime.**

## §4 — Where the project is, in six lines

**2,690 census folds live and searchable. 56 cohort targets scored at run 2 — `/scorer`,
`/target/:id`, attributions verified to `1e-12`.** ⚠ **No census row is scored, by `D-089`.**
**The clinical layer ships on two edges; HPA attribution went five-for-five after `PA` found five
surfaces, not two.** **Tranche 5's 776 rows are held by `D-091` ruling 2 — RENTAL SPEND — alone.**
⚠⚠ **`geom_proxy`'s status is genuinely unknown and is ordered.** **`F-042` (PAE discarded on 2,690)
is OPEN with a route now built and unmerged.**

## §5 — How this project works

**The log leads the code.** ⚠ A decision entry is written before the work it governs, and **a commit
message naming a decision does not discharge it.**
**Every claim names how it is known (`D-016`). Prefer the breakdown to the total.**
⚠ **Every absence is a category with a cause. Never a zero, never a blank.**
⚠⚠ **Two paths to one quantity, compared — ON THE NUMBERS.** *A matching percentage is not matching
counts.*
**Corrections are recorded, never patched away — including yours.** ⚠ **Planner errors go into the
log as Planner findings; the catch rate runs both ways and that is the signal that matters.**
**Production writes require the owner at the keyboard.** ⚠ **Never infer flag syntax.**
⚠ **A permission denial is stop-and-report. Never a retry, never a workaround.**
**Artifacts carry an AUTHORED-SHA256 with its RANGE, inside the file, above a range that excludes the
line carrying it.** ⚠⚠ **And select by HASH, never by filename — that has failed in both directions.**

## §6 — ⚠ What to say first

**Report §2's five items. Then state `PREWORK` §1's first job in one sentence — `QC1`, the `JA`
re-key — and name the two inputs rental waits on: the current unfolded set with its positives and
ceiling bands, and the one ~500 aa fold with the model released.**

⚠⚠ **Then STOP and let the owner steer.** **He asked for one thing. Give him that one thing.**
