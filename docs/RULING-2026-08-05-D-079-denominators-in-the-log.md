# RULING — 2026-08-05 — D-079's denominators enter the log **correct**, not superseded

> **COMMITTED to `docs/` as provenance. CITED BY the log, not restated in it — where this file and
> `docs/README.md` differ, THE LOG GOVERNS.** ⚠ This file records how a decision was reached; it is
> not itself authority. Check the `### D-NNN` / `### F-NNN` header, not a reference to it.

> **Answers Code's checkpoint-9 question. Binding on #1's docs-only commit.** Short by design.

---

## §1 — The question

The staged `D-079-…-v2.md` carries `non_surface` **2,211** and `unclassified` **2,795**, which
`RULINGS-2026-08-05-class-collision.md` supersedes with **2,209 / 2,793** plus `class_conflict` **2**.
Code asked whether the log entry states the superseded figures with the supersession marked, or omits
them and cites the ruling.

## §2 — RULING: neither, exactly. **The entry is written correct from the start.**

⚠ **This is not a supersession inside the log, because `### D-079` has never been in the log.** It is
a **first writing that happens to postdate six rulings.** Nothing is being quietly patched, because
nothing was ever published — the distinction that makes this different from D-079 v2's decision 2,
where a *ruling* was overturned and the rejected text was rightly retained so a reader can see the
trade was contested.

**A superseded quantity is not a contested decision.** 2,211 is not an alternative view of the census;
it is a number that was correct under a key the project no longer uses. ⚠ **Recording it in the
authoritative log — even marked — puts a wrong number where a reader will copy it**, and stale
literals reaching a surface is this project's most-repeated failure (`~5,102`, 2,886-as-denominator,
`COHORT_MAX_PLDDT`, the NECTIN4 score).

### What `### D-079` states

- **The four live denominators, in the entry:** `surface` **2,807** · `non_surface` **2,209** ·
  `unclassified` **2,793** · `class_conflict` **2** — each **stating its key** (distinct current
  accession), reconciling to **7,811**, ⚠ **never summed as a reportable quantity.**
- **`RULINGS-2026-08-05-class-collision.md` cited as the provenance of the fourth tag** — not
  restated. The entry says *that* the tag exists and *why*, and points to the ruling for the
  collision mechanism, the two named proteins, and the over-claim guard.
- **2,211 and 2,795 appear nowhere in `docs/README.md`.**

**⚠ The standing consequence applies to the entry itself.** D-079 cites today's six rulings; it does
not restate the key, the vocabulary, the collapse function, or the category set. Code's instinct on
this was right and it is now the ruling.

## §3 — The staged file must not be free to disagree with the log

The staged `D-079-…-v2.md` is committed today **and contains the superseded figures.** Two artifacts
naming one quantity, with nothing stating precedence — ⚠ **and F-011's landing note records what
happens next: a staged document pushed to the repository that never entered the log, cited as
authority for months.**

**Every staged 2026-08-05 document gains a landing header in the same commit:**

> **LANDED — `### D-079`, `docs/README.md`, 2026-08-05. Where this file and the log differ, THE LOG
> GOVERNS.** ⚠ §Denominator's `non_surface` and `unclassified` figures are **superseded** by
> `RULINGS-2026-08-05-class-collision.md`.

The superseded numbers stay visible **in the staged file**, where the ruling that corrects them sits
beside them. They do not enter the authoritative log.

## §4 — Migration state

Code's report is correct: an absence, not a value, blocking **#2 and not #1.** ⚠ **Owner action —
the `fly proxy` tunnel must be up before D-075.** #1 proceeds now.
