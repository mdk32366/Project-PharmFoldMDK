# PREWORK — 2026-09-04 — read `D-111` first, then the IGF2R fold, then walk the shipped surfaces

> **Written 2026-09-03 at close. Reads with `CLOSEOUT-2026-09-03-v3.md`.**
> ⚠ **SUPERSEDES both earlier drafts**, written before anything shipped.
>
> ⚠⚠ **GROUND ON A FRESH SNAPSHOT FIRST.** ⚠ **Yesterday this document's predecessor was wrong about
> the project's central result, a deleted file, and a PR title — all checkable in a minute — and a
> closeout said "nothing shipped" ninety minutes before two PRs landed.**
>
> ⚠ **State which REF every measurement came from.** ⚠ **A `docs/README.md` line number from a
> feature branch can be 166 off `main`.**

---

---

## ⚠⚠ CODE'S CORRECTION — measured 2026-09-04 against `origin/main`, after this document was written

> ⚠ **Inserted by Code at the owner's instruction. Nothing below is a ruling, a finding, or a
> drafted entry.** ⚠ **No sentence in this document was deleted or rewritten** — the statements
> corrected here are left where they stand, because a document wrong about its own day is what
> yesterday was about. **Where this block and the text below it differ, this block is the later
> measurement.**

### 1. ⚠⚠ `main` has moved TWICE more. It is `733c41f`, not `1d48d1d`.

    1d48d1d  D-111: hold-48 tiles at the T5 window (1656/128/1528), mucins out_of_class (#212)
    733c41f  D-112: TILE_WINDOW_AA lives in core.contracts; worker must not import hold48 (#213)

⚠ **Both are the owner's. Both have ONE PARENT — both were squash-merged.** ⚠ **No rule was broken;
`D-008 amendment 1` is still an unlanded draft.**

### 2. ⚠⚠ THE `D-` POINTER IS STALE AGAIN, AND WORSE THAN YESTERDAY

    Next free `D-` integer: `D-110`      ← the pointer
    headings present on main : D-110 · D-111 · D-112
    highest spent            : D-112

⚠⚠ **BOTH clauses of the invariant are violated: the pointer NAMES A SPENT HEADING (`### D-110`
exists) and it SITS BELOW the highest spent (`D-112`).** **Three entries were spent without the
pointer moving.**

⚠ **This is the same defect the `#209` merge corrected yesterday, recurred within hours.**
⚠ **Code changed nothing and names no finding.**

**The `F-` side is clean:** pointer `F-067`, highest spent `F-066`, `### F-067` absent — **HOLDS.**
⚠ **The guard Code landed still passes — `16 passed` — because it parses `F-` ONLY, by instruction.
That is exactly why it did not catch this.**

### 3. ⚠⚠ "Extending the invariant to `D-` is safe now" IS NO LONGER TRUE

**`PREWORK-2026-09-04-v3` §4.2 states:** *"Extend the invariant to the `D-` namespace. ⚠ **Safe
now** — the pointer reads `D-110`, so the check would pass."*

⚠⚠ **IT WOULD NOT PASS. It would turn `main` RED immediately.** ⚠ **Do not extend the invariant
until the pointer is corrected.** ⚠ **Correcting the pointer is the owner's; Code did not touch it.**

### 4. ✅ The prework's "read `D-111` first" question is ANSWERED. There is no silent contradiction.

`PREWORK` §1 and `BATON` §6 flag a possible conflict between `D-109`'s trained-context window
**1,026** and `#212`'s **1656/128/1528**, and say to check three things. **All three, measured:**

| check | result |
|---|---|
| does `D-111` **name** `D-109`? | ✅ **YES — 10 times.** |
| does it state the **1,026 vs 1,656** relationship? | ✅ **YES, explicitly** — see below |
| did the `RESERVED.md` `D-` pointer move in the same commit? | ❌ **NO.** That is item 2 above. |

**`D-111`'s own words, verbatim:**

> `D-109` (2026-09-02) recorded the hold-48 as spec-only and superseded #210's 1,656-window geometry
> with the trained-context window 1,026. ⚠ **A spec-only entry cannot outrank a later owner GO that
> names different numbers.**

**And it states what it does NOT touch:**

> **Does not amend:** `D-109` ruling 1 (repository namespace) · ruling 3 (mucins held on nature) ·
> ruling 6 (P11717's record is the pending row) · ruling 7 (stitched structures are not ranking-set
> eligible).

⚠ **So `D-111` is a BUILD GO that supersedes the window geometry deliberately, names the ruling it
supersedes, and lists the rulings it leaves standing.** ⚠ **The `F-065`-class concern the prework
raises does not apply. Read it, but do not read it as a conflict.**

⚠ **`D-111` also covers the IGF2R geometry the prework's §2 asks about:** IGF2R `P11717`, ECD span
**L = 2264** (the pending record; job 57 is the historical full-chain OOM — `D-109` ruling 6 /
`F-066`), giving **exactly 2 tiles**, `(1, 1656)` and `(1529, 2264)`, overlap **128**.
⚠ **Read `D-111` before recording anything from an external IGF2R fold.**

### 5. What is unchanged and still true

✅ Both PRs merged — `#211` → `c3cd142`, `#209` → `fe2ca69`, **two parents each**, all commits
individually reachable. ✅ Both deploys ran with `docs_only=false` and **succeeded**.
✅ `F-067` is **unspent**. ✅ **Squash and rebase are still enabled.**

⚠ **These three documents are now in `docs/` as UNTRACKED working-tree files.** ⚠ **They are placed,
NOT COMMITTED** — `main` is protected and no order authorises a PR. **The "session docs not in
`docs/`" open item is half-discharged, not closed.**

---

## §0 — What is already true, verified 2026-09-03/04

✅ **PR #211 → `c3cd142`** and **PR #209 → `fe2ca69`**, both **two parents**, both deploys ran with
`docs_only=false` and **succeeded**. ✅ **Pointers corrected to `D-110` / `F-067`.** ✅ **`/census`
and `/about` are live.**

⚠ **CODE, 2026-09-04: `main` has since moved AGAIN, to `733c41f` (`D-112`, #213). Both #212 and
#213 are one-parent squash merges.** The line below was true when written.

⚠⚠ **`main` has since moved to `1d48d1d`** — *"D-111: hold-48 tiling at the T5 window
(1656/128/1528) (#212)"*, the owner's, **2026-09-04 05:15**, carrying `core/hold48.py`,
`core/hold48_stitch.py`, `tests/test_hold48_tiles.py`. ⚠ **ONE PARENT — squash-merged.**

---

## §1 — ⚠⚠ Read `D-111` before anything else

**`D-109` ruled the tile window returns to the trained context — 1,026 aa, stride 898, 161 tiles
against 106 at the 1,656 window, verified by three independent methods.**
**`#212`'s subject reads `1656/128/1528`.**

⚠ **The Planner has NOT seen `D-111` and asserts NO conflict.** These may be different quantities —
a window vs a stitch geometry, or an amendment with stated reasons. ⚠ **But it is the first thing to
read, because a tiling geometry that contradicts a landed ruling without saying so is `F-065`'s
class.**

    git show origin/main:docs/README.md | grep -n "^### D-111" -A 40

**Check three things:** whether `D-111` **names `D-109`**; whether it states the relationship between
1,026 and 1,656; and whether `RESERVED.md`'s `D-` pointer moved to `D-112` **in the same commit**.

⚠ **`1d48d1d` has one parent.** ⚠ **No rule was broken — `D-008 amendment 1` is an unlanded draft** —
but that draft exists to forbid exactly this, and the constituent commits of #212 are now readable
only inside a squash body. **Whether that matters here is the owner's ruling.**

---

## §2 — ⚠ The IGF2R fold, running outside the pipeline

**A fold is in progress by a third agent. Whatever returns, these are the questions that decide
whether it can be used — and they are cheapest to answer while it runs, not after.**

**2.1 — ⚠⚠ WHICH SEQUENCE.** `F-066` is open on exactly this: job 57 OOM'd on the **full chain,
2,491 aa**; the pending row carries the **ECD span, 2,264** — **227 residues apart.** ⚠ **A structure
of the wrong one is not a slower answer to the same question; it is an answer to a different
question.** **Record the accession, the span start and end, and the residue count.**

**2.2 — ⚠ THE RECIPE.** `D-047`: the recipe resolves **at fold time from `TIER_RECIPE`**, never from
frozen `inference_settings`. ⚠ **A fold produced elsewhere has no `TIER_RECIPE` provenance unless
someone writes it down.** **Record dtype, chunk size, hardware, and the model revision.**

**2.3 — ⚠⚠ COMMENSURABILITY.** `F-012` measured that **chunk size alone moves coordinates and
pLDDT.** `D-109` ruling 7: a structure from a different path does **not automatically** enter
`F-004`'s ranking set. ⚠ **`core/manifest.py:196` already states it:** *"Assembly changes
`boundary_method`, so an assembled structure is not comparable to a single-pass one without saying
so."* ⚠ **An externally produced structure entering a ranking silently is `F-047` on a public
surface.**

**2.4 — What to write down while it runs.** Accession · span · residue count · dtype · chunk size ·
hardware · model revision · whether PAE was produced. ⚠ **Everything absent from that list becomes a
named absence with a cause, not a blank.**

---

## §3 — Walk the surfaces. They are live.

### `/census`

✅ every figure carries **date and artifact** · ✅ `folded` reads **2,690** — ⚠ **anything else means
the artifact was re-parsed, which was not authorised** · ✅ **planned** vs **in artifact** per tranche,
tranche 5 as **728 + 48** with its cause and the **2026-09-02** date — ⚠ **a bare `776` means it did
not land** · ✅ `P55073` as a **named absence with NO cause** — ⚠ **a cause means a contested question
was decided.**

### `/about`

✅ after *"Why this project exists"* · ✅ the standing line **first and in full** — ⚠ **a footnote, a
collapse or a trim is the defect** · ✅ **no number, no author, no journal** · ✅ *asks whether*, never
*shows that*.

### ⚠ The one you are meant to notice

**The Story's census sentence and `/census` will disagree.** Story is served live; the census page is
frozen at its artifact. ⚠ **Neither is wrong**, and `D-094 amendment 1` records it as **open by
ruling.** ⚠ **Do NOT fix it by making one match the other** — one loses currency, the other loses
provenance.

### ⚠ Four surfaces nobody has examined

**`/` Story · `/targets` · `/scorer` · `/method`** — ⚠ **inventoried only.** ⚠ **The Planner said
"every surface re-examined" and delivered three.**

---

## §4 — The three pins, now all safe

**4.1 — ⚠ Rename `test_reserved_next_free_is_f065_and_f050_still_reserved`.** ⚠ **The third instance
and the worst: the docstring was stale; the NAME is actively FALSE** — the test asserts an invariant.
⚠ **Identifiers are guarded by nothing.**

**4.2 — Extend the invariant to the `D-` namespace.** ⚠ **Safe now** — the pointer reads `D-110`, so
the check would pass. ⚠ **It would have turned `main` red yesterday.**

> ⚠⚠ **CODE, 2026-09-04: THIS IS NO LONGER TRUE AND IS THE MOST DANGEROUS LINE IN THIS DOCUMENT.**
> The pointer still reads `D-110`, but `### D-110`, `### D-111` and `### D-112` now all exist on
> `main`. **Both clauses are violated. Extending the invariant today turns `main` RED immediately.**
> ⚠ **Correct the pointer first — that is the owner's.** See the correction block at the top.

**4.3 — `F-062 amendment 1` closure.** ⚠ Status **OPEN**, and it says *both pins*. **Three exist.**

---

## §5 — Landings and rulings still open

1. **`F-067`** (`P55073`) · **`D-110`** (PAE + coverage `scope`/`foldable`) · **`D-008 amendment 1`**.
   ⚠ **Check the pointer first — `D-110`/`F-067` may already be spent by `D-111`.**
2. ⚠ **`CORRECTION 3` on merged PR #209 says "nine commits." It carried eight.** ⚠ **A Planner
   error, in a merged record.** Editing a merged body vs. leaving it with a correction elsewhere is
   the owner's.
3. ⚠ **The PAE 404 string vs `tests/test_pae_read_route.py:54`** — the test asserts `"2,692" in seg`;
   production measured **807 of 3,497**. ⚠ **They contradict. One must give.**
4. **The five oversized proteins** · **commensurability** · **MUC16** — ⚠ **and `D-111` may bear on
   the first two.**
5. ⚠ **1,105 local vs 1,099 CI.** ⚠ **Merge-ref explains 416→424, NOT this.** ⚠ **Six tests may exist
   that the gate never runs.** Cheap: `pytest --collect-only -q` both sides.
6. **`f826689`** · **credential attribution** · **Run B, still unowned.**
7. ⚠⚠ **No CLOSEOUT, PREWORK or BATON for 2026-09-03 is in `docs/`.** **This trio has never been
   committed.**

---

## §6 — ⚠ What NOT to do

- ⚠ Re-parse `census_features.v1.jsonl` or change `folded: 2690`.
- ⚠ Treat the Story/census divergence as a bug.
- ⚠ Squash any PR. ⚠ **Squash and rebase are still enabled and `1d48d1d` was squashed.**
- ⚠ Let the IGF2R structure enter any ranking surface before §2.3 is ruled.
- ⚠ Take `F-050` or `F-015`. ⚠ **Three instances of `F-050`'s class were found yesterday in one file.**
- ⚠ Trust a Planner draft's line number. ⚠ **The offset is 166.**

---

## §7 — The lesson worth carrying

⚠ **Yesterday's blocker was not the red test, the forked namespace, or the transport. It was a rule
the Planner invented — "the owner lands it" — which Code obeyed correctly five times.**
⚠ **When the apparatus stops repeatedly at the same gate, check who built the gate.**
