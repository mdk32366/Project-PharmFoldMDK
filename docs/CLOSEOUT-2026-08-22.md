# CLOSEOUT — Planner — session 2026-08-22

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts, **SECOND
occurrence of the marker**) = `d2f71bee8b7a268dd2ca164efb94a753bb795215ed68882da335e9ff044fee91`
**bytes** = `8328`

> ⚠ **DATE, STATED BECAUSE IT DIVERGES.** Log entries are stamped **2026-08-22**; the machine's wall
> clock stamped the commits **2026-08-20**. The precedent is `CLOSEOUT-2026-08-18.md` and
> `CLOSEOUT-Code-2026-08-21.md`, which name the same divergence. ⚠ **Named rather than reconciled.**
>
> ⚠⚠ **Where this file and `docs/README.md` differ, THE LOG GOVERNS.** Every number below is
> checkable and every one should be re-derived rather than quoted.

---

## §0 — Grounding

**Gate: 1,025 passed, 19 skipped** — identical to the count `CLOSEOUT-Code-2026-08-21.md` records.
**Parent invariant: `defined` 160 → 162** (`^### <CLASS>-<digits>`, D 101 · F 51 · DEP 6 · S 5).
**Amendment invariant: 35 cited / 35 defined, `cited − defined = 0`.** ⚠ **`reserved`, `cited` and
`dangling` were NOT reproduced** — **no committed script computes them.** `check_amendment_references.py`
is the only consumer of `RESERVED.md` under `scripts/`, `tests/` or `.github/`, and `gate.yml` has no
invariant step. **The `15 / 175 / 0` figures are quoted from Code, not measured here.**

**Landed:** `F-059` · `F-060` · `F-053 amendment 1`. **Pointer moved to `F-061` in the same change.**
⚠ **`F-050` remains RESERVED. Next free `D-`: `D-104`.**

## §1 — ⚠⚠ The day's largest finding: Phase 1 was already done before it was ordered

**`ZA` — the rental re-key that `HANDOFF-Code-rental-phase-1-COMPLETE.md` called *"NOTHING MAY BE
PRICED UNTIL THIS LANDS"* — had been run on 2026-08-19 and committed as
`MEASUREMENT-OUTPUT-2026-08-19-rekey-rental-and-JB.txt`.** It is `JA1`–`JA5`, the same structure the
order asks for, against the live database with `--database pharmfoldmdk` explicit.

⚠⚠ **This is `KEEL-3` demonstrated, and the boot document predicted it in its own header:** *"a long
session sheds its OLDEST context first, then answers fluently anyway."* **The outgoing Planner
ordered, in detail and with correct reasoning, work its own repository had already completed.**

**And the answer it was waiting for was decisive:**

> **`ZA4` / `JA4` — positives recoverable in the `441–629` band by climbing the ceiling.
> `FC` said 3. Current: 0.**

The unfolded cohort set is **3, not 29** — FAT2, MUC16, IGF2R — **none a Group B positive.** The
order's own stopping condition read *"if fewer than 3, the ceiling climb's justification weakens."*
⚠ **Zero is fewer than three. It did not weaken. It was removed** — and per `§6` of the boot, **a
stopping condition that REMOVES an option beats one that forbids it.**

## §2 — ⚠ `ZB2` was not measurable as written, and its premise was false

`ZB3` reasoned: *"Releasing the model frees ~5.24 GiB; the 439-aa incremental was ~1.26 GiB — so part
of the `441–629` band MIGHT fold locally."*

⚠⚠ **A fold requires the weights.** Releasing changes what is resident **between** folds, not the
peak **during** one, and the peak is what OOMs. **The two quantities are never in memory at different
times.** Recorded in `F-059` §3; **`F-053` §5 is left standing where it is written.**

⚠ **`F-053 amendment 1` had already caught this on 2026-08-20** — *"the incremental was measured at
439 aa and its scaling is UNMEASURED… trunk attention is at least O(L²)"* — **and it was never landed
in the log.** It is landed now, and **its prediction was correct: the fit is `L^1.983`.**

## §3 — What was ruled, and what was built

- **Owner: rental is SCOPED. `D-091` ruling 2 lifted.** All remaining tranches priced.
- **`F-059`** — the fold's incremental VRAM is **O(L²)**, fitted from **ten folds already committed**,
  cross-validated against an independent protein **to 0.01 GiB and one residue.** ⚠ **No fold was run
  for it.** It closes `D-077`'s unmeasured band **by calculation.**
- **`F-060`** — **the binding constraint on tranche 5 was never memory.** It is the model's **1,026 aa
  trained context**, which no purchase relieves. ⚠⚠ **The 2026-08-16 proposal found this in its §3 and
  never re-split the §2 cost table above it** — a correction that fixed the prose and left the
  arithmetic looking ratified.
- **`docs/PRICING-2026-08-22-all-remaining-tranches.md`** — **635 rows · 17–29 GPU-h · $9–23**, with
  the refit **pre-registered before any fold** and a **third branch named** (underpowered).
- **`docs/PREWORK-2026-08-23-the-fold.md`** and **`docs/ORDERS-Code-2026-08-23-the-rental-run.md`** —
  tomorrow's plan and its executable half. ⚠ **Tiling the entire past-context tail adds ≈$1;
  1,242 of 1,532 tiles fold LOCALLY for nothing.**

## §4 — ⚠ Corrections, including five of the Planner's own

**The catch rate runs both ways and that is the signal that matters.**

1. ⚠⚠ **A dangling citation, caught by the invariant on its first run.** The landing note for
   `F-053 amendment 1` cited **`F-052 amendment 2`, which is unlanded and therefore defined
   nowhere** — a forward reference to nothing, **inside a note about an unlanded amendment.**
   `F-044`'s shape. Corrected in the open, recorded in the note itself.
2. ⚠ **A hash-pinned artifact left unprotected**, caught by
   `test_every_hash_pinned_artifact_is_marked_minus_text`. `BOOT-Planner-2026-08-22.md` was moved
   into `docs/` carrying an `AUTHORED-SHA256` and **was not added to the `-text` block** — the test
   names it *"`F-047` member 15, recurring."* **The guard was right and the Planner had walked past
   it for six turns.**
3. ⚠ **Line endings asserted twice from a bad check.** `grep -c $'\r'` was used to test CRLF and
   **misfired in both directions** — it reported the README as LF when it is CRLF. ⚠⚠ **A clean
   number meaning something other than it appears is this project's most repeated defect, and it
   arrived here inside the check for it.** The fix was `od -c`, and **an `assert` refused the edit
   rather than guessing** — which is the only reason nothing was written wrong.
4. ⚠ **A miscount in the Planner's own prework, caught on re-read before merge.** §1 opened *"777
   single-pass rows"* while counting the 141 tiled rows among them. **The correct figure is 636
   single-pass folds plus 1,532 tiles.** Corrected and the artifact re-stamped.

5. ⚠⚠ **A silent pin break, written INSIDE the sentence warning about silent pin breaks.**
   `ORDERS-Code-2026-08-23-the-rental-run.md`'s header spelled the section marker out a second
   time — *"put any landing header ABOVE the first `## §` marker"* — which made the body the
   **THIRD** occurrence while the declared range says **SECOND**. ⚠ **A reader following the rule
   literally would have hashed from the wrong offset.** The tools used to write it are
   line-anchored and never saw it; **`test_every_discovered_document_carries_a_landing_header`
   reddened for an unrelated reason and reading that failure is what exposed it.** ⚠ Fixed by
   removing the second mention and saying why it is absent. **The other four artifacts were
   checked for the same defect and are clean.**

⚠ **Proportion, stated deliberately:** none of the five reached a fold, a spend, or a published
number. **Four were caught by instruments this project built for exactly that** — and the fifth
was caught by a test reddening for a DIFFERENT reason, which is luck, not a check. ⚠⚠ *The
second-occurrence rule has no guard. That is a gap, and it is named here rather than fixed
quietly.*

## §5 — ⚠ What is owed

- ⚠⚠ **`F-052 amendment 2` is STILL UNLANDED** — the `squeeze()` defect, in
  `docs/F-052-amendment-2-and-F-053-amendment-1.md`, hash-verified and paste-ready. **It was scoped
  out of today deliberately, not missed.**
- **`tile_max_aa` is UNRULED** — a **spend decision**, the owner's. The Planner's proposal is
  `1,026`, routed at `440`.
- **The per-tile manifest does not exist.** `tranche6_runs.py` computes runs and persists only
  aggregates.
- **The independent length guard remains OPEN** (`F-035` remedy 3). ⚠ `preflight` is unwired **and**
  guards the wrong axis. `F-059` now supplies the right one.
- **The 10 `no_domains` rows** need a boundary source or a stated exclusion.
- **No parent-invariant script exists.** ⚠ *It is checked by being re-run by hand, and this session
  could not re-run three quarters of it.*

## §6 — Tomorrow

**`ORDERS-Code-2026-08-23-the-rental-run.md`** (`RA` preconditions → `RB` free local tiles → `RC`
`ceiling_climb` on the rented card → `RD` the 878 rental folds). ⚠ **`RB` and `RD` run in parallel;
`D-090`'s claim-time tier filter is what makes that safe.**

⚠⚠ **`F-059` and `F-060` are one day old and both are OPEN. If tomorrow's measurements contradict
them, that outranks finishing the batch** — a refutation on day two is the cheapest one available.
