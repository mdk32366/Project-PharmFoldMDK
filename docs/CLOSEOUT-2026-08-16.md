# CLOSEOUT — 2026-08-16 — State of the world, and the rulings the Planner owns

> **For a reader who has been away.** ⚠ This is a **state** document: what exists, what every number
> is, what is running, and what is unresolved. **How each conclusion was reached is in
> `SESSION-2026-08-16.md`** — deliberately separate, so this one can be read for decisions.
>
> Where this file and the log differ, **THE LOG GOVERNS.** ⚠ This is provenance, not authority.
>
> **Provenance (D-016):** every figure is Code's reading, off the artifacts and the production
> database, at the time stated. **Nothing here is recalled.**

---

## §1 — ⚠ THE HEADLINE: the census is folding, in production, right now

**As of this writing, tranche 1:**

```
pending 690 · claimed 1 · complete 616     of 1,307
mean_plddt   min 12.9 · median 52.3 · max 77.5
⚠ below PLDDT_FLOOR 50.0 | 240 of 616 (39%)
recipe as recorded | dtype int8 · chunk 64 · driver 610.88
folds with NO recorded recipe | 0     ← a stop condition; has not fired
rows where fold_length != span_aa | 0
```

**`protein_analyses` 1,387 = 80 cohort (tranche 0) + 1,307 census (tranche 1).**
⚠ `ranking_runs` (5,5), `ranking_results` 5, `target_scores` 224 — **all unmoved. Nothing is scored.**

## §2 — Decisions taken without the Planner, and why that was defensible

⚠ **Almost nothing today needed new architecture.** The span vocabulary, the tranche column, the
fold path and the guards were all **existing design meeting reality**. Two decisions are genuinely
new, and **both were written before the code**, so the Planner is not behind on design — only on
state.

- **`### D-082`** — *a fold that exceeds VRAM must fail as a job, not as a bugcheck.* Three layers;
  ⚠ **the outermost is a driver setting we cannot query, so it reports `unknown`, never `ok`.**
- **`### D-083`** — *tranches are size-banded ascending, and the seed still governs inside each.*
  ⚠ **A tranche is a PARTITION; the seed is an ORDER WITHIN one. Batching touches only the first.**

**`### F-025`** was also written (the `no_topology` band reported **five** different things), and
**`### D-081`** freezes the 82 under the original span definition permanently.

## §3 — ⚠ THE OPEN RULINGS. These are the Planner's, and they are blocking.

1. ⚠⚠ **The numbering ruling is long overdue — SEVEN findings are queued behind one free integer.**
   The KEEL absence · a verification sharing an implementation with its subject ·
   derive-from-source-not-context · an order asking for confirmation invites confirmation · the
   `P0DKB6` yeast-vocabulary data-quality item · **`assert` doing guard work in two scripts** ·
   **a dry run that does not exercise its consumer's contract.** ⚠ `F-025` was claimed in a commit
   message before it appeared in any register and turned out free **by luck, not procedure**.

2. ⚠⚠ **Does `PLDDT_FLOOR` belong in ROUTING, not only in scoring?** **39% of tranche 1 is folding
   below 50.0.** Tranche 1 is the *shortest* band, so this is the worst case — but if it holds, we
   are spending GPU time on structures a scoring ruling would later exclude. **A 7–50 aa fragment
   has no tertiary context, so low confidence is the honest answer, not a defect.** ⚠ The decision
   is whether that is a reason not to fold them at all. **Numbers should come from the full tranche.**

3. **The fp16 arm of Task 4a was never completed** and ⚠ **cannot be, on this host, by that method** —
   the probe bugchecked the machine. The **overlap sample** (D-078's input) has not been drawn.
   ⚠ **`### D-078` remains unwritten and no cross-recipe comparison has been read.**

4. ⚠ **Layer 3 (fold-in-a-child) is not wired**, and layer 2 is **off by ruling** (uncapped, to get
   440 aa). **The crank's only protection is layer 1, which has never once been observed to fire** —
   no OOM has occurred under it. Owner ruled: wire it **before tranche 4**, which carries the rows
   nearest the ceiling.

5. **The census surface shows counts, not proteins.** ⚠ A browsable per-protein list is possible
   within D-079 dec 1 **but needs a ruling on which columns are permissible** — a list is one sort
   control away from being read as a shortlist.

6. **`known_good = 440` is confirmed uncapped on driver 610.88** and **unchanged**. ⚠ The true
   hardware ceiling is **above 456 and still unmeasured**; finding it means running without layer 2.

## §4 — The numbers, so nothing has to be re-derived

**Span definitions — ⚠ never compare across them without naming both.**

| | V1 `extracellular-substring` | V2 `ruled-vocabulary` |
|---|---|---|
| scope | **the 82, frozen permanently** (D-081) | the census |
| surface foldable | 2,352 | **2,581** |
| annex foldable | 332 | **886** |

**Census population:** roster **7,811** · surface **2,807** · annex **2,209** · manifest r7
**3,467 foldable**.

**Tranches (D-083):** 1 → 1,307 (1–50 aa) · 2 → 535 (51–150) · 3 → 517 (151–300) ·
4 → 332 (301–440) · 5 → 776 (>440, rental). ⚠ **Sums to 3,467. Tranche 0 is the cohort and is never
census.**

**Manifest identity:** seed `20260807`, `identity_fn_version 3`, revisions r1–r7 **all retained**.

**VRAM, driver 610.88, int8/chunk 64, `Q8WXD0`:** demand at 416 aa **6,527 MiB allocated** /
7,174 reserved; free budget **7,043 of 8,150** (the card drives a display). Ceiling **capped 0.85 →
416** · **capped + cache release → 432** · **uncapped → ≥456, limit never found**. ⚠ **The cap binds
on RESERVED, not on demand** — 440 aa needs 6,664 allocated, comfortably under the 6,928 cap, while
its reserved is 7,072.

## §5 — ⚠ What the webapp surfaces may and may not do

**May:** population counts, tranche batches, absences with causes, the limitation block, span
definitions named.

⚠ **May NOT, without a ruling:** show a score or rank for any census row (**D-079 dec 1**, enforced
by `ranking_run_id = NULL` and the scorer-import refusal) · present census and cohort in one list
(⚠ **75 accessions collide**) · compare a V2 count to a V1 count without naming both.

⚠ **And one thing the next architect must know:** `input_value` is **no longer unique**. Any new
read keyed on accession must state which population it means, and `tests/test_no_census_leak_on_tranche_zero.py`
will red if it does not.

## §6 — Rig state

**Tunnel** up on `:16380`, verified by a real query — ⚠ **attached to a Claude Code shell session,
not a durable window.** **Worker** live, polling, PID pair (venv stub + base interpreter), ⚠ started
with `HF_HUB_OFFLINE=1` **without which job 1 dies on the model load**. **Gate** 637 passed /
15 skipped. **Branch** `census/task3-spans`; `main` at the PR #134 merge.
