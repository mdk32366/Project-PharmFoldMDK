# Pre-Work — 2026-07-25

> Grounding SHA: **`b7d5e4b`** (main, after #67 / D-046 harness). Confirm it hasn't moved before
> starting: `git log --oneline -1` on main. If D-047 merged overnight, the head advances and this
> pointer moves with it.
>
> Read first, in order: yesterday's closeout (`CLOSEOUT-2026-07-24-rerun.md`), then D-045/D-046 in
> the log, then this. The rerun firefight is context for almost everything below.

---

## §0 — Settled last session (no action needed, recorded for grounding)

- **Rental is torn down.** PAE secured (4/4), D-045 provenance verified in stored records, pod
  terminated, 50 GB volume deleted, **$0.00/hr confirmed.** No billing carries into today. Nothing
  to rent, nothing to fold — this is a code-and-UI session.
- **IGF2R resolved — documented A6000 ceiling.** Chunk-invariant OOM (11.84 GiB tried at both
  chunk-64 and chunk-32, byte-identical); chunk-16 not attempted as it would fail identically. Final
  cohort state: **4 of 5 rerun targets folded** (ADAM17 72.78, NOTCH2 57.89, SDK1 58.01, PTPRZ1
  30.68), IGF2R a characterized ceiling. This is a result, not an open item.

---

## §1 — D-047: code the cure (top priority, gates future reruns)

Ruled last session, spec'd in `ORDERS-Code-D-047-fold-time-recipe`, **not coded.** The recipe must
be resolved at **fold-time** (`build_fold_spec`, `app/artifacts.py:47`) from the current
`TIER_RECIPE[job.meta["tier"]]`, not read from the job's frozen `inference_settings`. This cures the
*class*: no enqueued job can carry a stale recipe again.

**Log leads code — write D-047 first**, in two parts: (1) the incident (requeue replayed the
pre-D-042 frozen recipe; the manual `UPDATE` fix; the worker/requeue race), and (2) the cure (recipe
authoritative at fold-time; the provenance tension — frozen recipe *was* arguably intent, but the
fold already records what it *actually* ran, so provenance lives in `fold_provenance`, not the job's
pre-fold settings).

**The regression test that names the bug** must go red-then-green: a job with stored
`inference_settings.chunk_size = None` but `meta.tier = rental` → `build_fold_spec` yields
`chunk_size = 64`. Watch for the `TIER_RECIPE` import: it lives in `core/enqueue.py`; if importing
it into `app/` drags heavy deps into the serving tier, relocate it to `core/contracts.py` (already
serving-safe, holds `FoldSpec`) — and keep `test_image_contents.py` green (no worker/torch in the
image).

**Trap:** don't let D-047 try to *rescue* last session's jobs. It's the cure for *future* runs. The
four (five) already-folded/failed jobs are done; IGF2R's retry uses the manual path because D-047
won't be deployed in time to matter for it.

---

## §2 — IGF2R: resolved (context for the UI and the ceiling story)

IGF2R is **done — a documented A6000 ceiling** (see §0). No further fold attempt is queued. Two
downstream uses of this result:

- **The UI (§3) should render IGF2R's `failed` state with its reason**, not as a blank — D-043
  already does this; confirm the provenance/coverage views show the chunk-invariant ceiling honestly
  rather than as an unexplained gap.
- **It strengthens the domain-folding case (§4).** IGF2R now joins MUC16/FAT2 as evidence that
  whole-sequence folding has a hard size ceiling on available hardware — three data points, not two.
  The finding is precise and reusable: *the failing allocation is chunk-invariant, so it is a
  length-driven cost the triangular-attention chunking does not mitigate.*

**If the owner ever wants IGF2R folded**, it is a deliberate spend decision (a ≥80 GiB card, e.g.
H100/A100-80G), made against the honest ceiling result — not a reflexive re-rent. Not queued.

---

## §3 — UI-depth §3.1: the provenance panel (deferred from last session)

Harness (D-046) is ready. Build tests-first against the **two-population** shape (D-045): pre-D-045
folds render the environment as *not captured* (never as `—`, never as a value); post-D-045 folds
(the rerun's) render complete. Fixtures use the **real** values now known from the live pod:
`torch 2.8.0+cu128`, `NVIDIA RTX A6000`, `cuda 12.8`, `transformers 5.14.1`. Three render rules and
the "no completeness score" omission are specified in D-046 §3. Delete the `bandFor` smoke test in
this PR once `Provenance` has real tests (owner ruled: delete).

**New, concrete input for this panel:** the cohort now spans **three fold recipes** —
local (int8/chunk-64), the 38 rental (fp16/unchunked), and the rerun (fp16/chunk-64, and IGF2R at
chunk-32 if it lands). The provenance panel should make the recipe legible, not bury it. A fold's
`chunk_size` is now part of its honest story (see the whole §2 saga for why).

---

## §4 — Enrichment: cancer associations + the excluded giants (ruled, needs entry + curation)

Two coupled, cohort-shaped pieces, both ruled last session, neither built:

- **Per-target cancer associations** — curated CSV in `data/`, one row per target, each with a
  citation, no live PubMed/CDC retrieval, no incidence statistics. Purpose: "the why" — let the
  reader's own sense of relative cancer severity carry the gravity. Needs D-048(a) + the **owner's
  curation pass** (which associations are established enough to state flatly is a domain judgment,
  not Claude's to make). Migration trigger to record: *moves to a DB table only if it becomes a
  scorer feature or a query filter.*

- **Domain-level folding for the excluded giants — D-048(b), new methodological work.** MUC16
  (14,451 aa, CA-125 — the primary ovarian-cancer biomarker) and FAT2 (4,030 aa) are D-022 named
  exclusions: unfoldable *as one sequence* on any card. But they're modular. Domain-by-domain
  folding is how the field handles them and would be a real extension — **and MUC16's clinical
  weight makes it the strongest motivation in the cohort.** This is scoped, non-trivial (domain
  boundary definition is itself a scientific claim — SMART/Pfam vs. the ECD annotation), and should
  NOT be conflated with the rerun. Its own entry, its own decision about boundaries.

  These two pair naturally: MUC16 is both the poster child for "why the associations matter" and the
  poster child for "why domain folding is worth doing." A reader who learns CA-125 is the top
  ovarian-cancer biomarker *and* that it's too big to fold whole understands both the stakes and the
  method limit in one target.

---

## §5 — Stale-doc corrections (small, owed, do when convenient)

- `RUNBOOK-rerun-5-targets.md` — token check `69` → **64** (or "matches Fly secret length").
- `GUIDE-renting-the-a6000.md` Step 7 — the `pip install -r worker/requirements.txt` line fails on
  a 2.8.0 pod (torch pin is 2.11.0+cu128, local-only). Document the "fold on the pod's torch,
  install the rest" path that actually worked.
- Consider whether `worker/requirements.txt` should stop pinning a `+cu128` local build that no
  rental pod carries — or split into "GPU stack (image-provided)" vs. "the rest (pinned)."

---

## §6 — Sequencing

Teardown and IGF2R are settled (§0) — this is a **code-and-UI session**, no rental.

1. **§1 D-047** — the fold-time recipe cure. Log first, tests-first, merge. Gates every future
   rerun; nothing else in the fold path is safe to assume until it lands.
2. **§3 provenance panel (UI-depth §3.1)** — harness (D-046) ready, fixtures now use the real
   live values. Tests-first, two-population render, delete the smoke test in this PR.
3. **§4 enrichment** — needs the owner's curation pass; can run in parallel with Code on §1/§3.
4. **§5 doc fixes** — whenever.

**Note:** the owner intends to finish the **UI layer** this session (their words at last session's
close). §3 is the spine of that; the remaining UI-depth items (§3.2 tier legibility, §3.3 band
recompute against the now-larger cohort, §3.4 per-residue distribution) follow it and should be
pulled from the original UI-depth pre-work once §3.1 lands.

**The through-line:** last session proved the *mechanism* (D-042 chunking, D-044 requeue, D-045
capture) all work — but the *recipe never reached the fold* because it was frozen at the wrong time.
§1 is the structural fix that makes the mechanism actually reachable. Everything else is the UI and
enrichment that make the folds *legible* — which, after watching a 30.68 pLDDT and a 14,451 aa
exclusion, is clearly where the honesty of this project lives.
