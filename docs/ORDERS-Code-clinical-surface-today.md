# ORDERS — Code — the path to cancer associations on the surface TODAY, and the four measurements that get us there

> ⚠ **TO BE COMMITTED to `docs/`.** ⚠⚠ **PROVENANCE CHANNEL CHANGED — see §8. Do not retype this.
> The FILE is downloaded and committed; the paste is for reading only.**
>
> ⚠ Planner grounding is `7011e24`; `HEAD` is `b7ecc2a` and the survey branch is `8f8b20f`. Every
> repository statement here is **as of `7011e24`** and is a question.
>
> **No GPU. No rental. No fold. No production write.** **Tranche 5 stays HELD** — `D-091` ruling 2.

---

## §0 — Accepted from the survey, and one of them is the session's result

**`CA` accepted.** ⚠ **`d0fd95e` pre-registering the bar BEFORE the fetch makes the ordering checkable
from history rather than asserted** — that is the pre-registration discipline working, not described.
Three matching `sha256` values, `INS` resolving to exactly one detected row of 103, `ZZZ_NOT_A_GENE`
returning zero. **A bar with a negative case that could have failed.**

**`CB` accepted, and reading BOTH paths is why we know anything.** ⚠⚠ **`Q3MIW9` MUCL3 genuinely
disjoint — one path could never have shown this.** Taking HPA recorded as a **choice**; the disjoint
case **left unresolved**. Correct on both counts. **`CB3` sums to 3,467.**

**`CD` accepted on form** — each population summing to its own denominator, ⚠ **the folded set and the
manifest not pooled.** *`F-011` and `F-016` were different mechanisms and were never pooled.*

⚠ **`§8.5` correctly NOT written.** A rendering test with no renderer is the vacuity `AC3` named.

---

## §1 — ⚠⚠ THE PATH TO THE SURFACE TODAY. It is a sequence, not a delay

**`D-093 (the clinical association layer)` is a pre-registration and is VOID IF CODE PRECEDES IT.**
That clause is the entry's own and it is not waived by momentum. ⚠ **We spent 2026-08-18 unwinding
`D-091` ruling 3 from the other direction.**

**Four steps. Three of them run in parallel and none of them waits:**

| step | who | gated on |
|---|---|---|
| 1 — `CC`, decision 6's three answers | **Code, §2** | nothing — start now |
| 1 — `DB`, `DC`, `DD` measurements | **Code, §3–§5** | nothing — start now |
| 2 — **`D-093` amendment 2** | **Planner** | step 1's `CC` |
| 3 — owner ruling on the amendment | **Owner** | step 2 |
| 4 — schema · ingest · surface | **Code** | ⚠⚠ **step 3, and not before** |

⚠ **Step 4 is the only gated one, and steps 1–3 are hours, not days.** **The surface can land today.
It cannot land first.**

⚠⚠ **AND ONE CLARIFICATION THAT WILL OTHERWISE COST A TURN: the prohibition is on BURDEN, not on
EXPRESSION.** `D-093` decision 1 bars a **burden** statistic from protein-level payloads. **An
expression edge — protein → tumour IHC — is not burden and is not barred.** **Do not over-apply the
prohibition and refuse to render the thing the layer exists to render.**

---

## §2 — Task DA — write `CC`: decision 6's three questions, in decision 6's own format

**Yes, draft it as a document for the owner to rule on.** It is the direct input to the amendment.

- **`pathology.tsv` and `normal_tissue.tsv` separately.** ⚠ `pathology.tsv`'s reproduction is already
  established — **cite `D-100`, do not re-derive it.**
- ⚠⚠ **Put the Level-vocabulary and reliability findings INSIDE question (1) — *what the supplier
  actually serves*.** **They are facts about what the data IS, not caveats about how we use it**, and
  filing them as caveats is how they get skimmed.
- **State the mapping under question (3) with its pinning**: the cached UniProt HPA cross-reference,
  the choice of HPA over Ensembl **recorded as a choice**, the 288 subset cases, and `Q3MIW9`
  unresolved.

## §3 — ⚠⚠ Task DB — `CD3` cannot be read yet, and your own numbers show why

**785 all-20 · 1,148 none · 91 exactly one is the session's most interesting result and it is not yet
a measurement.**

**DB1 — ⚠ "Reaching" has no stated rule.** Any detection? Any `High`? A quasi-H-score above a
threshold? **Report the distribution at SEVERAL bars, not one** — at minimum *any non-zero detection*,
*any `High`*, and *`qh ≥ 150`* (Kathad's, for comparability). ⚠ *A single setting is a dial wearing
the costume of a measurement*, and `F-043`'s flip rates were withdrawn the day they were published
for exactly this.

**DB2 — ⚠⚠ DECOMPOSE THE 1,148.** Against `pathology.tsv`, `2,328 + 15 + 1,008 = 3,351`, so **at most
1,023 of the 1,148 are explained by absent gene or empty panel — leaving roughly 125 with IHC PRESENT
that still reach zero.** **Those are two entirely different facts under one bar**, and it is the
defect `CA4` names one paragraph earlier: *`Not detected` is explicit, so a missing row means NOT
TESTED.* **Report the 1,148 by cause, summing, with the key.**

**DB3 — Report the same decomposition for the 785.** ⚠ **Saturation has causes too**, and *reaching
all 20 at any-detection* is a different object from *reaching all 20 at `High`*.

## §4 — Task DC — the Level vocabulary, and the reliability asymmetry

**DC1 — ⚠⚠ Does `pathology.tsv` carry the same non-ordinal values?** `N/A`, `Ascending`,
`Descending`, `Not representative`. **Report its full distinct-value set with counts.**

⚠ **This lands on `P-004` item 3.** That item argues Kathad's 1-2-3 weights are a linear scale on a
roughly logarithmic quantity. **If HPA's own Level vocabulary is not a clean ordinal, the objection
is stronger than the one currently written** — not *the weights are arbitrary* but ***the thing being
weighted is not consistently ordered.*** **`P-004` needs the answer before it claims either.**

**DC2 — Verify the reliability asymmetry is a real schema difference**, not a file-variant artifact
of these two downloads. ⚠⚠ ***"Reliability is present in `normal_tissue.tsv` and absent from
`pathology.tsv` — the modality driving target selection is the one without the quality flag"* is
publishable as written**, which is precisely why a reviewer will check it first. **Report the column
lists side by side, and whether HPA documents the difference anywhere.**

## §5 — Task DD — `CE1`, closed properly

*"Equal counts are not the same set, and I did not verify set identity."* ⚠ **That is two paths to one
quantity compared on totals rather than members — the defect class itself, named in the act of
committing it.** **Compare the sets. Report any member present in one join and not the other.**

---

## §6 — ⚠ PREVIEWED, NOT ORDERED — the surface contract the amendment will rule

**Read it now and object now.** Nothing here is authorized until step 3.

1. **Expression edges render; burden does not exist.** `burden_supplier_unlicensed` is a **named,
   visible category** wherever a burden would have appeared — ⚠ **never a blank, never a zero, never
   an omission.**
2. ⚠⚠ **Every rendered association carries its evidence type**, and `differential_expression` is
   labelled as such — **never as "associated with".** *Every surface protein is expressed somewhere.*
3. **Absence is a category with a cause, and there are FIVE**, from `CD`: `ihc_present` ·
   `ihc_gene_absent` · `ihc_panel_empty` · `hpa_absent` · `accession_ambiguous`. ⚠ **The same
   underlying fact arrives as a different category in the two files — 1,008 empty panels against
   1,023 omitted genes — and the surface must not launder that into one word.**
4. **The n travels with every number.** ⚠ Panels are median **11**, max **12**, and **246 of 1,640
   rows are at n ≤ 4** in the Kathad grid.
5. **`therapeutic_precedent` renders with its circularity warning in the same frame** — the GPI badge
   pattern, attribute and liability together, never as a positive signal.
6. ⚠⚠ **`D-094` mount preconditions apply, and `F-048` is on these same pages.** **A cancer
   association rendered beside a five-residue span must not become a second place where that span
   looks authoritative.**
7. **Four-part HPA attribution, all parts together**, including the version-pinned
   `v22.proteinatlas.org` URL. **A bare "Human Protein Atlas" string does not discharge it.**

---

## §7 — Numbering, and no new integer

⚠⚠ **`D-093`'s five gate assertions never existed at `b7ecc2a`, and the entry describes them under
*"written before any code."*** **A pre-registration asserting a test surface that was never built is
`F-047`'s class at the level of a decision entry** — well-formed, confident, and unfalsifiable from
inside itself.

- **`F-047` member 8** (Planner — `D-093` is Planner-authored). ⚠ **Found only because the order said
  *report whether each exists* rather than *implement these*.** Had it said the latter, you would have
  built them and nobody would have learned the entry had been claiming them for two days.
- **The correction is recorded in `D-093` amendment 2**, attached to the entry it corrects.
- **The five tests you proved RED are the remedy, landing AFTER the finding, not instead of it.**
- ⚠ **No new integer.**

## §8 — ⚠⚠ The authored hash: the remedy is replaced, not retried

**Two consecutive landings, same cause. You were right not to hash your own transcription** — a
`HASH-MATCH` computed against your own retyping certifies nothing.

**The diagnosis: the channel reflows, so byte-identity is unreachable through it, so an authored hash
can never match. The fix is not a better hash — it is that PASTE and PROVENANCE are two channels and
the Planner collapsed them.**

- **PASTE is for reading and acting.** Fast, and reflow does not matter.
- ⚠⚠ **The FILE is for the record. The owner downloads the artifact and commits the file itself,
  never retyped** — exactly as `f8210dd` handled the four hand-carried artifacts, **which worked.**

**Then the authored hash means something, because the bytes never pass through a reflowing channel.**
**`F-047` member 3's remedy is superseded, and the two failures are recorded as what proved it wrong.**

## §9 — Report

Branch, tip, ⚠ **the number and title of any entry landed in the message that lands it**, the
invariant with its keys, and the gate without `.env` sourced.
