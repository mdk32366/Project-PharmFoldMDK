# CLOSE-OUT — 2026-09-16 (session run 2026-09-15 PDT) · The 37 re-attached, the three collapsed, `0014` applied

**Owner:** Matt Kelly · **Planner:** Claude Opus 5 · **Builder:** Code
**Governs:** `ORDERS-Code-2026-09-16-reattach-and-collapse.md`, Amendments 1–10.
**Session date:** **2026-09-15, America/Los_Angeles (PDT, UTC−7)**, measured (`date`: 09:06 PDT at the
start). ⚠ The documents are labelled **09-16**. That mismatch is recorded in §6, not smoothed.

**`main` ends at `f7d0a9d`** (the #330 merge, 2026-09-15T23:11:39Z). Pipeline green on all three jobs;
**Fly release v237** deployed.

**Next free integers:** **`D-168`** · **`F-081`** · **`A-032`** (`docs/RESERVED.md`).

---

## 1. What landed, each claim with its artifact

| # | what | evidence |
|---|---|---|
| 1 | **`F-079`** — the collapse shipped without `D-159`'s identity check, and the guard enumerated by hand | PR #326, merged `844871f` |
| 2 | **`D-167` step 1** — the read-only Phase B read, 16/16 expectations met | PR #327 merged `51fea11`; `data/control/d167/state_before.json`, sha256 `b825065d…80b7` |
| 3 | **`D-167`** + **`F-080`** — the re-attach design and the done-witness finding | `2634ca3` |
| 4 | **`D-167` amendment 1** — role preamble, ownership gate, session question, the zero-count rule | `4a3084f` |
| 5 | **Phase C code** — `d167_reattach.py`, `d167_volume_capture.py`, `core/db_role.py`, the collapse's role gate | `5e8f25e`; PR #328 merged `ee55298`; CI `postgres` 40 passed |
| 6 | **Phase D** — capture → re-attach → collapse → `0014`, owner at the keyboard | `data/control/d167/phase_d/00–09`, `capture.json` `88820b82…93bb`; commits `63f0807`, `2085cad`, `66b7ca1` |
| 7 | **Phase E read** — one read-only tunnel, exit 2 on a mis-specified key | `phase_e_read.json` `4d8b4886…10ec`; `phase_e/e2–e5`; `a57ea4d` |
| 8 | **E.1 / E.2** — identity 37/37 from the committed blob; the owner's walk 5/5 PASS | `phase_e/e1`, `e6`; `4f447ff` |
| 9 | **`F-077` am. 2, `F-078` am. 3, `D-166` am. 1** — collapse executed, debt paid, `0014` applied | `4f447ff` |
| 10 | **E.3** — the PAID notice, the reminder deleted, the evidence EOL-protected | `794612c` (RED), `cd1923c` (GREEN); PR #330 merged `f7d0a9d` |

**The three production writes, with their witnesses:**

| write | witness | value |
|---|---|---|
| re-attach 37 + 37 | `d167_reattach.py --witness`, jobs 4389–4905 complete with a `pdb_path` | **480 → 517** |
| collapse 3 + 3 | `DUPLICATES_SQL`; public API | **no row**; kept 200, dropped 404 |
| `0014` | a **separate** `alembic current` | `0013_cancer_burden` → **`0014_enqueue_identity_unique (head)`** |

**Independently checked by Code, outside the scripts:** 37/37 served structures hash to `capture.json`;
served `fold_provenance` equals the captured file on four sampled rows; the committed `capture.json` blob
hashes to the value the machine printed.

---

## 2. The census now has three keys, and every citation must say which

| key | before | after | why it moved |
|---|---|---|---|
| **row-keyed** `run '1'` AND complete | 3,651 | **3,648** | the collapse deleted three duplicate tile rows **inside** this key |
| **identity-keyed** `(accession, tile_start, tile_end)` | — | **3,576** | cohort-82 × census overlap: 75 shared accessions − 3 non-complete cohort rows = **72**. ⚠ Consistent, **not proven** |
| **census proteins**, one per accession (the page) | 3,467 / 3,463 folded | **unchanged** | representatives are lowest-id Run-1 rows; neither write touches them |

---

## 3. Error accounting

### 3.1 Planner — 8 recorded
1. `completed_at` claimed one clock domain; it is two.
2. Truncated the affected-lines scan and presented it as found (A7.2).
3. Predicted a ~+1–2 s clock residual from whole-second data; measured ±0.08 s.
4. Miscounted the changed test files (12 for 13).
5. Wrote a PowerShell runbook without the encoding fix (A6.1).
6. A6.1's own affected-line table was incomplete (A7.2).
7. **E.1's census witness expected an invariance the sitting was designed to break** (A8.2).
8. **A8.3 expectation 3 could not be met**: an identity key with no population term (A9.1).

### 3.2 Code — 7 recorded
1. **A placeholder `$url` reached step 1.** Authentication failed before any session existed; nothing read. Remedy: a shape check before connecting.
2. **Two tool-blocked commands and one unparsable commit message.** Each stopped before executing; nothing was staged. Remedy: commit messages via `git commit -F`.
3. **A stray `C:\tmp\d167_capture.json`** written by a test expected to fail at its only write. Identified by content and removed. **Expecting a failure is not containment.**
4. **`gh run watch` exited 1 mid-run** and was replaced by polling. No step was taken on it.
5. **The `LIMIT 50` diagnostic printed as a count** in the Phase E read — a truncated read presented as a size, in the instrument written to prevent exactly that.
6. **A README line committed without scanning that file** (only `01a` was scanned). Rule taken: every file in a commit is scanned before that commit.
7. **A probe that could not fail:** the CRLF check matched every line, so it reported "counts identical" for files that differed. The byte-level read found the truth.

### 3.3 ⚠⚠ The shared defect class, named once (A9.3)
**A truncated or vacuous read presented as a measurement.** Planner 2, 4, 6, 7; Code 5, 7. It appeared in
both parties' work, on the same day, including inside instruments written to catch it. **Every count
states its key; every capped list says it is capped; every probe states what it returns when the thing is
absent.**

---

## 4. ⚠ The rule caught a live instance of its own defect
`capture.json` and `state_before.json` were **CRLF in the working tree** — rewritten by git during an
earlier branch checkout, while no `.gitattributes` rule covered them — so their authored sha256 failed on
disk while the blobs stayed intact (`F-047` member 15, the fourth instance). Restored from the blobs under
`-text`. All three evidence files now hash to their authored values on disk **and** in the index.

---

## 5. Owed, carried forward

### 5.1 ⚠⚠ FIRST ITEM NEXT SESSION — the population-aware read (A9.2, verbatim)

| reading | expectation |
|---|---|
| **R1** | count of `(accession, tile_start, tile_end)` groups with > 1 complete `run '1'` row, **read with its own `count(*)`** | **72** |
| **R2** | every one of those groups has exactly **one** `cohort_tranche = 0` row and exactly **one** tranche ≥ 1 row | **72 of 72** |
| **R3** | identity key **with** population (`cohort_tranche = 0` vs `≥ 1`): groups with > 1 complete row | **0** |
| **R4** | the three named overlap exceptions each have no complete tranche-0 row and exactly one complete census row | **3 of 3** |

⚠ Any R1–R4 miss is a **finding**. R3 ≠ 0 is a genuine same-population duplicate, and it bears on the
Run-2 identity design that blocks slice 4. ⚠ Before that read, the population tranches are confirmed from
`db/models.py` and the enqueue scripts, **not** from the amendment.

### 5.2 Owner
- **Rotate `fly-user` and `WORKER_AUTH_TOKEN`**, then drop `DATABASE_URL` from `.env` (A8.6). ⚠ Until
  rotation, the literal-secret checks are the only thing keeping the public repo safe.
- Rule `D-168` (§5.4) and the paper's §4.3 loss statement, which now narrows to **zero rows**.

### 5.3 Code
- **`F-079` stays OPEN:** `backfill_run_label.py`, `census_ingest_features.py` and
  `clinical_ingest_edges.py` still owe `assert_campaign_target`.
- **`D-166` §4b:** the Run-2 identity is undesigned; **slice 4 stays blocked** (A1.5).
- ⚠ **Whole-protein identity is indexed by nothing.** `0014` is partial over tile identity only.
- Fix the `LIMIT 50` diagnostic: its own `count(*)`, the list labelled capped, and a test that a capped
  list is never printed as a count.
- **Make printed output ASCII**, `d166_collapse_duplicate_tiles.py:328` first (after an irreversible
  commit), with a test that a piped run under cp1252 exits 0.

### 5.4 Ruled next: `D-168` — features on assembled structures
`ORDERS-Code-2026-09-16-feature-coverage.md` §2.2 asks for a new refusal category
`refused_assembled_structure`. ⚠ **One already exists and ships:** `refused_assembled_incommensurable`
(`core/structural_profile.py:53`, `app/census_profile_read.py:128`, `ui/src/components/CensusTable.jsx:212`,
pinned by four test modules). **A second name for one thing is `F-049`'s family** — see
`PREWORK-2026-09-17.md` §3.

### 5.5 Smaller, named
- The `{'job_id': '...'}` placeholder printed in the dry-run plan.
- `.env` still points `DATABASE_URL` at the dead port 16380.
- No Fly health checks are defined: "healthy" means `started` and serving.
- `working_tree_clean` excludes untracked files; the name should say so.
- `esmfold_local` (`db/models.py:120`) vs `esmfold` (the app's write): two names for one thing.
- The next-free integer is pinned in **13** test files.
- **The citation invariant's residual:** `D-168` is cited by the next-free bars with no row, and all three
  pointers report NO ROW. Unchanged all session. **Accept it in the open or fix it** (A4.7.3).
- ⚠ **The date-label mismatch:** this session ran 2026-09-15 PDT under documents labelled 09-16. Every
  document states its own frame from here on.

---

## 6. Method notes earned today
1. **A calibration is about the environment it ran in, and it names that environment.** The first C.0 run
   passed because Code's tool injects `PYTHONIOENCODING`; the owner's terminal has no such variable.
2. **Half a fix can be worse than none.** `PYTHONUTF8` alone stopped the crash and wrote mojibake into the
   evidence — exit 0, no traceback, wrong bytes.
3. **A stop rule that fires on its own description gets ignored.** A6.5 fired three times on the prose of
   the reports describing it; A10.3 narrowed it to literal secrets and credential shapes.
4. **Evidence is verified from the blob, not the working tree** (§4).
5. **An owner-gated write is the owner's, including the paste.** Code never passed `--i-am-the-owner`, and
   the one link no test could exercise — `flyctl` quoting — was proven by the owner running it.
