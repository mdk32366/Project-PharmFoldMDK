# PREWORK — 2026-08-21 — rental, and only rental

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts, **SECOND
occurrence of the marker**) = `68b19cff2ccad379ff665bdc588ba0c42da6ffc0dd62ca185f2c8c34c3645080`
**bytes** = `5082`

> ⚠ **Intent, not state.** Companion: `CLOSEOUT-2026-08-20.md`. **THE LOG GOVERNS.**
> ⚠⚠ **The owner's instruction for this session: *"All I want to do tomorrow is the rental."*** **The
> board was cleared for that. Do not open a second arc.**

---

## §0 — ⚠ Read before planning

1. ⚠⚠ **Ground from the LOG, not from doctrine, and not from documents ABOUT entries.** **The Planner
   twice reasoned about `D-075` from a proposal about `D-075`, and asserted `geom_proxy` unbuilt in
   three documents while `F-017` records it as run id=5.**
2. ⚠ **Check the resource before assuming its class.** **Three times this week a resource assumption
   was refuted by one file or one measurement** — extraction as GPU work, `preflight` as wired,
   *"well inside `CEILING_KNOWN_GOOD`"* at 0.03 GiB free.
3. **A count states its key · a hash states its range · a claim states how it is known.**
4. ⚠ **Send entries at the right HEADER LEVEL.** `###` top-level, `####` amendment. **A top-level
   entry landed at `####` is cited-but-not-defined.**
5. ⚠⚠ **Never accept a default** — `--database`, `PYTHONHASHSEED`, `straddle`, the interpreter.

---

## §1 — ⚠⚠ THE FIRST JOB, AND IT IS A WALL: `QC1`

**`D-091` ruling 2 reads as *tranche 5 is held*. It holds RENTAL SPEND, and rental spend has TWO
claimants** — **tranche 5's 776 rows (coverage)** and **the cohort's remaining unfolded rental targets
(⚠ statistical POWER on a fit with ~3 positives per parameter).**

⚠⚠ **NOTHING MAY BE PRICED UNTIL `JA` IS RE-KEYED.** **`FC`'s bands — 13 in `441–629` with 3
positives, 16 at `≥630` with 3 — were computed against `D-041`'s STALE 29, and 26 of those turn out
already folded and scored.** **We were one turn from pricing a purchase partly already made.**

⚠ **Census feature extraction is NOT a claimant** — stdlib CPU, five minutes, done.
⚠ **`F-042`'s remediation is NOT a claimant** — **every census span is 1–439 aa against a 440 ceiling,
so it is LOCAL card time (~7–8 h), not rental money.**

## §2 — ⚠⚠ The cheapest measurement on the board, and it may remove the reason to spend

**ONE FOLD AT ~500 aa WITH THE MODEL RELEASED. Two minutes.**

**`F-053`: `CEILING_KNOWN_GOOD = 440` is a LENGTH. What binds is ~1.26 GiB incremental against a
5.24 GiB RESIDENT model** — **and the model is resident BY POLICY, not by physics.**

⚠⚠ **If releasing it opens the `441–629` band, part of the rental bill disappears — and `FC` put 3 of
6 recoverable positives in exactly that band.**

⚠ **`F-053 amendment 1` names the hole in that hypothesis and it must be read with it: the 1.26 GiB
was measured at 439 aa and its SCALING IS UNMEASURED. `span^1.26` is a TIME law and says nothing
about memory.** **Trunk attention is at least O(L²).** ⚠⚠ **So the hypothesis is a QUESTION, not a
reason to spend or not spend, until that one fold runs.**

**And `preflight` still guards the LENGTH axis, is consulted by nothing (`F-049`), and
`ceiling_climb.py` runs outside it** — ⚠ **`D-082`'s blood line is a host bugcheck.**

## §3 — ⚠ What rental actually buys, in two currencies

**Coverage:** 776 census rows. ⚠ **Power:** up to 6 more Group B positives against a current 12, on a
7-parameter fit. **These are different purchases at the same price and `QC1` prices neither until it
reports.**

⚠⚠ **And a refit is NOT pre-committed anywhere (`FC3`).** **A refit chosen after seeing the rental
folds is post-hoc.** **If the point of renting is power, the refit must be ruled BEFORE the folds
land, not after.**

## §4 — ⚠ Live but NOT this session's subject

**Merge `fix/pae-squeeze-and-read-route` @ `692e805`** → unblocks `SA4`/`SA6`, which decides whether
`P-006` exists. **`geom_proxy`: has it already run?** — ordered, unanswered. ⚠ **The 3Dmol viewer
defect, ordered 2026-08-19, still never reported.** **`F-042` path A/B/C. The backbone graphic spec.
The census profile surface.** **Credential unrotated; backup present, not restorable-verified.**

## §5 — ⚠ Three things measured this week that will be re-derived wrongly if not read

1. ⚠⚠ **The census is SHORTER than the cohort** — spans **1–439 aa** against `ecd_length` **13–1,652**.
   **Every rental intuition built on FAT2 at 4,030 aa and MUC16 at 14,451 aa is about the COHORT's
   tail.**
2. ⚠⚠ **PAE is MORE confidence, not less.** **It fixes *pLDDT answers the wrong question for
   multi-domain proteins*. It does NOT fix *the score rides on the model's self-assessment*** —
   `F-051`, 38.6% of attribution on the confidence pair. **`geom_proxy` is the instrument for the
   second problem; PAE is not.**
3. ⚠ **The census structural profile is blocked on a RULING that is now written** — `D-079` amendment
   ‹N›, **46.7% refused / 53.3% computable** — **and the hard part is the SURFACE, not the ruling.**

## §6 — What to say first

**Report the grounding block** — snapshot hash and what it cannot prove · highest `### D-` and `### F-`
from the headers · **both invariants WITH THEIR KEYS** (last: parent **155 / 15 / 170**, amendment
**29 / 29 / 0**) · §1 in your own words · **and ask Code what landed since the snapshot.**

⚠ **Then stop and let the owner steer.** **He has asked for one thing today. Give him that.**
