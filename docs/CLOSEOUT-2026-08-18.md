# CLOSEOUT — session 2026-08-18 (log entries stamped 2026-08-17, wall clock)

> **State, and what is open.** The companion is `PREWORK-2026-08-19.md`, which is what to do next.
> ⚠ Where this file and the log disagree, **THE LOG GOVERNS.**
>
> ⚠⚠ **The Planner asserted, four times, that a document written this morning did not exist.**
> That is §1, and it is not buried — a closeout that leads with the wins and hides the failure is a
> marketing document.
>
> ⚠ **Naming:** ruled today — log entries carry **wall clock**; documents carry the **session name**.
> This session opened with `PREWORK-2026-08-18.md`, so it closes as `CLOSEOUT-2026-08-18.md` while
> every entry it produced is stamped **2026-08-17**. Stated rather than reconciled.

---

## §1 — ⚠⚠ The Planner reasoned from a stale snapshot and asserted absence

**In four separate messages, including closing recommendations, the Planner stated that the tranche 6
design document was unwritten and that 776 rows were held behind an absence.**

**`D-095` had been written that morning, by Code, at `061eb3f`.** It is the tiling design document.
The Planner's own scoping ruling — drafted as `D-095`, renumbered on merge — is **`D-098`**.

- **The snapshot was not stale on delivery.** It was clean and current at the previous close.
  ⚠ **`D-095` landed during the session, while the Planner worked from the snapshot.** Drift
  accumulates *during* a session, not only before it.
- ⚠⚠ **Code said so twice** — *"D-098 widened tranche 6, so D-095 is now scoped wrong"* is a sentence
  that asserts D-095 exists. **It was read as a numbering error, because it conflicted with a premise
  nobody was examining.** Being told and not hearing is worse than not being told.
- **The invariant could not catch it.** `cited − defined − reserved` proves an entry exists and has
  no access to what it says. **This is `F-044` in its worst form: the Planner did not misread an
  entry, it asserted the absence of one it never checked for.**

**Answered by KEEL V9** — Principle 8 gains *you cannot assert absence from a stale copy*, and
KEEL-2 Step 22. ⚠ **A doctrine amendment is not a control. It will be skipped under momentum, which
is exactly when this happened.**

---

## §2 — ⚠ `F-043`'s numbers were withdrawn the day they were published

**The entry reported 83 / 33 / 116 one-move flips. Code could not reproduce 83 under any of five
candidate rules, including the one the entry appears to describe.** The prose said *one patient
reclassified by one category*; the arithmetic was a distance test. **The entry stated neither.**

⚠⚠ **The Planner had, the same day, twice required Code to fix this exact defect** — A3 re-run under
three type sets and reported at five values of *k*, on the ruling that *a single choice is a dial
wearing the costume of a measurement.*

**Withdrawn same day, before external use. `F-043` amendment 2 pre-registers five rules.**
⚠ **The 52-at-exactly-150 survives** — it depends on no perturbation rule, which is why it was ruled
the number that leads. ⚠ **And the surviving list itself carried a refuted figure**: `>150` gives
**285**, not 286; the sign is worth **52** pairs. Corrected in an attributed note, not silently.

---

## §3 — What was established, and it is substantial

**The Kathad comparator is reproduced exactly.** `D-100`: convention A — `qh = 100 × (Low/total +
2·Medium/total + 3·High/total)`, denominator **including** Not detected — reproduces **337 / 337**
and correctly excludes **1,303 / 1,303**. ⚠ **The convention was read off the file, not recovered by
matching** (`percent_law == 100 × Low/total`, 1,640 / 1,640), so `F-022` does not bite.

**The census supplier is validated end to end.** v22 `pathology.tsv` — 401,800 rows, 20,090 genes ×
20 cancers — reproduces S3's grid **1,640 / 1,640, all four count columns identical.**
⚠ **S3 is a verbatim extract of it.** Acceptance was by reproduction, not by version label.

**`F-043`** — panel median **11**, max **12**, **246 of 1,640 rows at n ≤ 4**; ⚠⚠ **52 pairs at
exactly 150.0**, the modal discrete value of the estimator; 8 more flip on the paper's own printed
percentages. **Framing settled by the authors' own limitations section: they state the small-sample
caveat and the cutoff caveat separately and never connect them.**

**`F-042` settled by observation.** The control fold (`D-099`, `cc2551f`) ran **25 local int8
chunk-64 folds — the census recipe — and PAE was emitted on 25 of 25**, dimensions matching. So the
2,690 are `pae_absent_local_tier`: **the model produces it on every forward pass and the pipeline
discards it.** ⚠ **And the 79 cohort files RESOLVE** — bijection, no orphans either direction, no
stubs. Decision 3 closed; the finding stays open.

**Task A, 141 rows.** ⚠ Best exact boundary agreement anywhere **58.0%**, and the headline **moves
with the type filter** (58.0 / 55.8 / 47.0 at k=0). **33.6% of span residues sit in no domain.**
**10 rows have no UniProt domain at all.**

**Task L.** ⚠⚠ **The two-regime split does not survive**, `multiple_oversized_runs` is **0 of 141**,
and **only 6 of 141 have an oversized run.**

**KEEL V9 issued** — seven documents, carrying v7 (never issued), V8, and this session's four
Planner-side rules. **Eleven principles**, a fifth named document, a three-players section, and
**Part C** for scale. ⚠ **The two unchanged documents were edited in place and validated at +1
paragraph each**, so *"inspected, unchanged"* is a checked claim.

---

## §4 — ⚠⚠ D-095 is written, PROPOSED, and not ruled — and the session changed it

`D-091` ruling 3 requires *written **and ruled***. **It is not ruled, and 776 rows are held behind
it.** Four things measured today bear on it and none was known when it was written:

1. ⚠⚠ **Its headline is inverted.** **135 of 141 tile at gaps and sever nothing; 6 need one
   `run_interior` cut each.** It reads as a document about cutting cadherin stacks and is a document
   about tiling at gaps with a six-row exception.
2. ⚠ **Decision 1's stated basis expired.** It chose UniProt *"by availability"* because `F-041`
   established InterPro's UniProtKB cross-reference carries no coordinates — **0 of 22,176 xrefs.**
   **Task A2 then fetched the instrument F-041 named as missing.** The choice may stand; the reason
   cannot. ⚠ **`F-041`'s deeper point survives and should carry it instead: the disagreement is
   between models, so no arbiter exists** — `ECO:0000255` on **416 of 521** features, `ECO:0000269`
   **zero times**.
3. **Population is 141** (`D-098`), not the 10 it was measured on.
4. ⚠⚠ **Tiling does not avoid rental; it changes the bill.** `known_good = 440` at int8 against a
   trained context of 1,026 means **every tile over 440 aa needs a rental card** — so `tile_max_aa`
   is a **spend decision**, and `D-091` ruling 2 holds all rental spend. **D-095 nowhere says so.**

**Owner rulings taken today:** FAT1–4 stay **in scope** and are folded, with the cut made **legible**
— ⚠ `tile_cut_kind` gains a fourth value **`run_interior`**, and under `D-094` its disclosure is a
mount precondition. `tile_max_aa` is **measured, not proposed**. **D-095 is HELD** pending Task L.

---

## §5 — The 275 residues: a definitional divergence reported as a reconciliation

Task A counted **180,802** residues in domains; Task L counted **180,527**. Both partition 272,302
correctly. ⚠ **33.6% vs 33.7% is not agreement — it is two numbers rounding to neighbours.**

**Cause, fully accounted: three straddling domains.** Task A **clips** them into the span; Task L
**drops** them. 312 raw residues, 37 absorbed by the union → **312 − 37 = 275. Exact.**

⚠ **Code's own diagnostic reproduced the defect while explaining it** — *"I added raw lengths where
the quantity is a union"* — and caught it only because it printed both.

**Ruled: CLIP in both.** A clipped straddler occupies its residues; dropping it manufactures a gap at
the span boundary that does not exist in the molecule, ⚠ **and a phantom gap is a phantom cut site.**
**Not yet applied.** ⚠ **Record `straddle_handling` beside `merge_rule` and its gap tolerance —
three unstated parameters on one derived object in two days. A run is a construction, not an
observation.**

---

## §6 — Log entries this session

`D-095` (tiling, Code) · `D-096` · `D-097` · **`D-098`** (tranche 6 scoped to the 141) ·
**`D-099`** + amendment 1 (control fold) · **`D-100`** (Kathad reproduction) ·
**`D-093` amendment 1** (HPA is CC BY 4.0; ingest is **column-scoped**) ·
`F-041` (Code) · **`F-042`** + amendment 1 · **`F-043`** + amendments 1 and 2 · **`F-044`** (Code).
**Invariant holds: 143 defined, 15 reserved, 157 cited.** Gate 755.

---

## §7 — Open, and who holds it

| item | holder |
|---|---|
| ⚠⚠ **`D-095` amendment — the critical path** | Planner |
| `D-101` — the PAE comparison pre-registration | Planner |
| ⚠ **`F-009` correction** — Kathad name TROP2/HER3/CLDN18.2 themselves; *"false negatives the paper missed"* is unsupportable for Trop-2 | Planner |
| `F-045` — the wrong-but-plausible-answer family | Planner |
| **Task L re-run under clipping**; ⚠ **is `single_run_only` disjoint from `oversized_run`?** | Code |
| `F-043` re-derivation, five rules × two constraint modes × four populations | Code |
| **Tasks G, I, J, K** — census IHC coverage, stage 3, kill-stage, flip rate at scale | Code |
| The equivalence run (census vs survey), citation question first | Code |
| ⚠ **Cluster / backup verification — ORDERED, NEVER RUN.** Does `zp2wjrej9lwodn4q` have a completed backup? | Owner + Code |
| ⚠ **Rotate the DB credential** — printed in a transcript 2026-08-17 | Owner |
| Old cluster `gjpkdonnmkeoyln4` — ordered destroyed, **unverified** | Owner |
| ⚠ Does `core/span_definition.py` match `RULINGS-2026-08-07-span-definition.md`? | Code |
| KEEL V9 — adopt, and delete the V6 deck once read | Owner |

---

## §8 — Habits that earned their keep today

- ⚠⚠ **Assert that a string replacement applied.** It fired three times — a schema-order error, a
  deck fragment that said *"ever goes in a file"*, and a `sldIdLst` move. **Each would have shipped
  silently.**
- ⚠ **Look at the render, not the code.** A cloned slide inherited its source's footer — true,
  well-formed, correctly styled, about the wrong subject. **No check caught it; looking did.**
- ⚠⚠ **Accept by reproduction, not by label.** Five wrong files in one day, every one real
  well-formed data from the right organisation. **Row counts caught them; a 1,640-row reproduction
  settled it.**
- ⚠ **Bucket three ways.** A single total would have lost the PAE control; the by-tranche cut carried
  it, and a schema assumption inside the query was exposed by it.
- ⚠ **Two paths to one quantity, compared — on the NUMBERS.** A matching percentage hid a 275-residue
  divergence for a full turn.
