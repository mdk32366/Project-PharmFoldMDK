# PREWORK — 2026-09-16

> **Read this before anything else. Hand it to Code before Code plans anything.**
> **Grounding commit: `a8b3b33`** (main at the 2026-09-15 close).
>
> ⚠⚠ **THIS DOCUMENT IS IN THE REPOSITORY ON PURPOSE.** Every prior pre-work and close-out lived in
> a Downloads folder, and `D-162` rule 8 names that as one of the five faces of *state outside
> version control determining a result*. The item below that must survive the night was, for most
> of 2026-09-15, gated only by a notice that prints at the end of a six-hour fold.

---

## 1. ⚠⚠ OWED AND TIME-SENSITIVE — restore slice 2's 37

**This is item one and it is the only thing here that can be silently forgotten.**

Jobs **4869–4905** were set `tier = NULL` on 2026-09-15 so slice 3 could be the only claimable
population (order B, owner ruling). ⚠ **A NULL-tier job is claimable by nobody and
`refuse_on_strangers` deliberately does not count one — so nothing in this system will ever raise
its hand about these rows.** If the restore is skipped, **slice 2 stays 480/517 for ever** and
no check anywhere will say so.

**The debt, in full:**

```
python scripts/f078_null_tier_the_37.py --restore                    # dry run
python scripts/f078_null_tier_the_37.py --restore --i-am-the-owner   # the write, OWNER TYPES IT
```

1. `jobs 4869–4905`: `tier NULL` → `'local'` (37 rows)
2. then `--preflight`, then fold them as their own bounded population
3. ⚠ Job 4869 was already returned `claimed` → `pending`, so it needs nothing further.

**Done looks like slice 2 reading 517/517.** ⚠ When it does, **delete
`tests/test_f078_owed_restore.py` in the same commit** — its removal is the record that the debt
was paid.

⚠ **They must be folded as their own population.** Slice 3 and the 37 are different bands (31–100
against 29–30), and mixing them would contaminate slice 3's per-band transport measurement, which
is the campaign's whole purpose.

## 2. ⚠⚠ CORRECTED — the hazard is CONCURRENCY, not repetition. See `D-166`.

⚠⚠ **What this section said on 2026-09-15 was wrong, and it is recorded rather than silently
overwritten** (`D-129-C`). It read: *"the enqueue path is not idempotent… if that enqueue were
re-run for any reason, `D-159` passes every assertion and writes 1,097 duplicates."*

**Measured before it was acted on.** Re-running slice 3's `--enqueue` dry run against the live
cluster, after today's 1,097 rows, reports:

```
band 31-100: 1101 rows
  already carry a Run 2 row: 1101
  to enqueue: 0
nothing to enqueue; the band is already covered.
```

⚠ **`scripts/task4_slice3.py` filters by `_existing_run2` before it writes.** A sequential re-run is
a **no-op** and always was. The stated hazard does not exist.

⚠⚠ **The real hazard is narrower and worse.** `_existing_run2` is a **read, then a decision, then a
write**, with no lock between them — and so is `core.hold48._emitted_tile_idents`. Two enqueues
running **at the same time** both read "not present" and both write.

**That is not hypothetical; it is what produced `F-077`'s three duplicates:**

| | measured |
|---|---|
| the two enqueue transactions | `21:41:55Z` and `21:44:21Z` — **2 m 26 s apart, overlapping** |
| proof they overlapped | `jobs.id` is a non-transactional sequence; **A took 3694 and 3697 AFTER B took 3693** |
| the guard | present, type-correct, and **five hours old** (`52ecb61`, 2026-09-04T16:36:54Z) |

⚠ **`F-077` §3 read the ~2 h 57 m between the pairs' `completed_at` as the emitter re-running.**
It was one GPU draining a serial queue with 20 folds in between. `F-077` amendment 1 records the
correction; `F-047` amendment 6 records the class.

⚠⚠ **Owed, and it is `D-166`:** collapse the three duplicates (**owner at the keyboard**), then a
`UNIQUE` index on the tile identity and the Run-2 identity, then `alembic upgrade` (**owner at the
keyboard**). The index **cannot build while the duplicates exist**, which is the check.

⚠ **Do NOT re-run an enqueue concurrently with another one.** Until the constraint exists, that is
the whole mitigation, and it is a discipline rather than a guard.

## 3. Where slice 3 stands

Folding unattended at the 2026-09-15 close: **132 of 1,097, all `ok`**, `free_before` **7043
constant**, wall mean **20.5 s**, ~5.5 h remaining.

**On completion:**

```
python scripts/task4_slice3.py --report      # disk only, no tunnel, no database
```

⚠ The report **prints the item-1 debt** — that is the gate, and it is why it must not be deleted
until the 37 are folded.

**Report shape** (`§G.3`): `ok` count · `free_before` series, **expect 7043 constant — a departure
is a finding** · artifact sizes · **no zero-byte rows** · the stop condition reached, named ·
**wall time and the transport term** as `(elapsed − fold_time) / n_folds`.

⚠⚠ **Two measured points do not licence an interpolation.** 4.7 s/fold at 251–384 and 0.7 s/fold at
1–30 is a factor of 6.7; `D-157` amendment 4 exists to retract exactly that move. **Carry neither
value into this band.** The script encodes it as `TRANSPORT_S = None`.

⚠ **`progress.csv` timestamps are LOCAL (UTC−7); the database is UTC** (`F-078`). Any comparison
between them converts explicitly, and states that it did.

## 4. Queued, none blocking

| item | note |
|---|---|
| **KEEL V10 into the repository** | the six documents are in project outputs and **not in the tree** (`D-163`). ⚠ **`F-075`'s amendment waits on this** — it cites KEEL-3's multi-day clause and KEEL-2 Step C5, neither verifiable from the repo |
| **`F-075` amendment** | the three-party shape, Planner instance included. ⚠ **Owner rules; Code transcribes.** Blocked on V10 above |
| **`P-001` commensurability** | amendment 2's third outcome, `underpowered` at n = 4. ⚠ **The owner's, and the methods section cannot close over it** |
| **The freeze line** | ⚠ **unruled, and now the only thing between this and a stable done state** |
| **Lessons Learned** | predates 2026-08-17 and needs both truncation incidents |
| **`.env` credentials** | `fly-user` password and `WORKER_AUTH_TOKEN` both appeared in a transcript and are **unrotated** |
| **IPv6 on the Wi-Fi adapter** | disabled during the 2026-09-13 recovery; not re-enabled |

## 5. The paper — three drafts exist

`docs/DRAFT-P-001-{methods,results,discussion}-2026-09-15.md`, committed and also in Downloads.
⚠ **Drafts. Nothing in them is ruled**, and each names its pending items.

Written during slice 3's fold. **The census is verified intact at 3,651 of 3,651**, `F-072` is
untouched, and `F-078`'s loss is confined to Run 2 instrument measurement — so the paper stands
where it did on Saturday, now measured rather than asserted.

## 6. State

- **main:** `a8b3b33` · local suite **2,619 passed, 0 failed**
- **Next free:** **`D-166`** · **`F-079`** · **`A-032`**
- **Live cluster:** `kyzl60xz9zyrpj9g`, marked. ⚠ **`zp2wjrej9lwodn4q` is the forensic record —
  do not destroy, do not connect for convenience.**
- **Tunnels:** none open at the close.

## 7. Standing rules (`D-162`, and they now number eight)

1. Never run `pytest` in a shell where `.env` has been sourced.
2. `--image` redeploy before a full rebuild for secret-change deploys.
3. A documented blind spot is a **blocking defect**, not a comment. ⚠⚠ **And it is NOT about
   safety systems** — the qualifier was excusing everything else, and `D-162` rule 3 is amended to
   drop it. `D-165` is the same rule on a *determinism* property; `D-166` on an *idempotency* one,
   where the written-down limitation was a **test that only ever ran the sequential case**.
4. `127.0.0.1`, not `localhost`, in tunnel `DATABASE_URL`.
5. ⚠ **A port number is not a cluster identity.** Bind by name, corroborate against `fly mpg
   status`'s Direct IP, one tunnel at a time, close it when done.
6. A matching total is not a matching set. **A matching set is not a matching state.**
7. A verification probe states what a success proves, **and what it would return if the thing were
   absent.**
8. Anything outside version control that determines a landed artifact is a defect on the same
   footing as a single-copy enumeration.

⚠ **Production writes require the owner at the keyboard** (`F-075`). An authenticated session is not
an attestation, and a Planner instruction cannot discharge the gate.
