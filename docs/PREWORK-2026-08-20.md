# PREWORK — 2026-08-20 — rental tranches, and the budget that has three claimants

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts) = `0cf8c0975f6042ee745b6c8b006c3304daeaedc921d1496269392c9600b3c7a1`
**bytes** = `6764`

> ⚠ **Intent, not state.** The companion is `CLOSEOUT-2026-08-19.md`. **Where either disagrees with
> `docs/README.md`, THE LOG GOVERNS.**
>
> ⚠⚠ **`PREWORK` NOW CARRIES A HOLDER COLUMN.** A live production defect sat in `PREWORK-2026-08-19`
> §5c through a full session that produced eleven log entries, **because the closeout has a holder
> column and the prework did not.** **One column on a document already written daily. §6.**

---

## §0 — ⚠ Read this before planning anything

1. **Ground from the LOG, not from doctrine.** ⚠⚠ **Four of yesterday's Planner defects were
   corrections issued from KEEL without searching `docs/README.md` first** — one of them corrected a
   correction, and the log had ruled both halves. **Search before you correct.**
2. **Cite by number AND name**, and ⚠ **cite a reservation by number and INSTANCE, never by class.**
3. ⚠ **Never assert absence.** Name the artefact and its revision, or ask.
4. **A count states its key; a hash states its range; a claim states how it is known.**
5. ⚠⚠ **`--database`, `--interpreter`, `straddle` — name the target explicitly. Never accept a
   default.** **A default is a dial that does not announce itself.**

---

## §1 — ⚠⚠ THE FIRST JOB: the budget has THREE claimants and they have never been in one frame

**`D-091` ruling 2 reads as *tranche 5 is held*. It actually holds RENTAL SPEND — and rental spend is
now claimed by three different pieces of work.**

| claimant | what it buys | measured? |
|---|---|---|
| **Tranche 5 — 776 rows** | census coverage | ⚠ released from the design gate by `D-095` amendment 1 |
| **The cohort's unfolded rental targets** | ⚠⚠ **statistical POWER on the fit, not coverage** | `JA` **incomplete** |
| ⚠⚠ **Census feature extraction over 2,690 folds** | **the precondition for any structural profile** | **never run — `KB3`** |

⚠ **The third one is new as of yesterday and it changes the shape of the day.** *"Score the census"*
turns out not to be a scoring task at all: **zero of 2,690 census rows carry a `protein_features`
row.** **Six features over 2,690 folds is compute, and it competes with the other two.**

**⚠⚠ Nothing may be priced until `JA` is re-keyed.** `FC`'s bands — 13 in `441–629` with 3 positives,
16 at `≥630` with 3 positives — **were computed against `D-041`'s stale 29, and 26 rental targets
turn out already folded and scored.** **We were within one turn of pricing a purchase partly already
made.**

## §2 — ⚠ The cheapest move is blocked, and the dependency is not obvious

**The local ceiling is bracketed at `(440, 630)`: `440` known-good, `441–629` UNMEASURED.**
⚠⚠ **If the true ceiling is near 600, part of the rental bill disappears** — and `FC` found **3 of 6
recoverable positives sitting in exactly that band.**

**But `scripts/ceiling_climb.py`'s own docstring says it runs OUTSIDE `vram_guard.preflight`, and
`F-049` established `preflight` is written, tested, and consulted by nothing.** ⚠⚠ **`D-082`'s blood
line is a host bugcheck from over-allocation. The one measurement that would shrink the bill is the
one currently running with no guard at all.**

**Sequencing, and it is a recommendation not a ruling:** ⚠ **wire `preflight` → re-key `JA` → climb
`441–629` → then price what remains.** **Every step changes the price of the next.**

## §3 — ⚠⚠ Scoring, and the question the owner opened yesterday

**Two things wear the name and the closeout separates them:** the cohort **is** scored and live —
run 2, 56 targets, `/scorer` and `/target/:id`, attributions verified against production to `1e-12`.
**The census is not, by `D-089`, and cannot be, by `KB3`.**

**`D-079` amendment 1 is RULED and holds the shape:** ⚠ `structural_profile`, **never a score, never
ranked, REFUSED where the standardizer is out of range**, ⚠⚠ **and never re-entering the cohort's arc
— `P-001` is unanswerable if it does.**

**Two things the owner flagged for a fresh look:**
- ⚠ **`sasa_normalized` NEGATIVE and `largest_patch_fraction` NEGATIVE** — *more exposed surface and a
  larger contiguous patch both push a target DOWN.* **Ruled not a defect; wants a second look.**
  ⚠⚠ **`largest_patch_fraction` carries 24.0% of attribution, so whatever it means, it is doing a
  quarter of the work.**
- **`F-051`'s fork** — is the dominant `membrane_proximal_plddt` share **circularity (A)** or
  **informative regional uncertainty (B)**? ⚠ **The measurement favours B. `geom_proxy` is the
  instrument. `P-001` settles it, not a conversation.**

## §4 — ⚠ Unblocked and waiting on hands

**`ED` — the ingest.** ⚠⚠ **The `D-100` bar runs INSIDE the transaction and rolls back on any
mismatch** — 337/337, 1,303/1,303, 1,640/1,640 with four count columns identical. **Proven able to
reject. A wrong ingest cannot land.**
**`EE` — the surface**, gated on `ED`: ⚠ **per-datum HPA deep links to `v22`, not a footer**, and
*"never displayed in the absence of such citation"* is HPA's own mount precondition.

## §5 — ⚠ Three that decay while nothing goes red

- ⚠⚠ **The 2026-08-17 credential is unrotated** — owner's standing decision, made knowingly,
  recorded. **Only rotation ends it; time does not.**
- ⚠ **Backup: PRESENT, not restorable-verified.** `gjpkdonnmkeoyln4` is deleted, **so the recovery
  path actually used in August no longer exists.**
- **The HPA email**, deferred knowingly. ⚠ **If the answer is *ShareAlike applies*, the surface will
  already be public.**

---

## §6 — ⚠⚠ OPEN ITEMS, WITH HOLDERS. This column is the point of this section

| # | item | holder |
|---|---|---|
| 1 | ⚠⚠ **Re-key `JA`** — current unfolded set, positives, ceiling bands | Code |
| 2 | ⚠ **Wire `preflight`** — blocks the ceiling climb | Code |
| 3 | **`D-095` amendment 2** — the 10 rows with no legal cut site | Planner |
| 4 | **`F-047` amendment 2** — members 22 (schema-vs-data), 23 (binary pre-registration on an unverified precondition), 24 (`input_value` not unique) | Planner |
| 5 | **The guard-direction sweep — `F-050`, still unwritten** | Planner |
| 6 | ⚠ **`F-049`'s third instance** — `ranked` 67 vs 56 | Code |
| 7 | **`ED` ingest** | Owner + Code |
| 8 | **`EE` surface** | Code |
| 9 | ⚠ **`JA5`** — the 13 `no_span_measured`, still uncaused | Code |
| 10 | **`D-093` amendment 4** — clause 3's composition error, `EC1`'s three layers | Planner |
| 11 | ⚠⚠ **3Dmol viewer defect** — `BF1`–`BF4`, ordered, **status unknown** | Code |
| 12 | **Credential · backup · HPA email** | Owner |

⚠ **Item 11 is the reason this table exists.** **It was ordered, it has never been reported, and
nobody noticed for a day.**

## §7 — What to say first

**Report the grounding block — snapshot hash and what it cannot prove · highest `### D-` and `### F-`
from the headers · the invariant WITH ITS KEYS · this file's §1 in your own words · and ask Code what
landed since the snapshot.**

⚠ **Then stop and let the owner steer.** **Yesterday produced eleven entries and four Planner defects,
and the defects clustered where the momentum was highest.**
