# CLOSEOUT — session 2026-08-20

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts, **SECOND
occurrence of the marker**) = `e898c0317bf0a6d7cbe34d7710d932fa16b309426e3d54234fbe1ae053297b56`
**bytes** = `6588`

> **Companion: `PREWORK-2026-08-21.md`.** ⚠ **Where this file and the log disagree, THE LOG GOVERNS.**
> ⚠⚠ **The Planner's largest error today was over-caution rendered as fact, and the owner caught it
> by asking whether he was committing a crime. That is §1.**

---

## §1 — ⚠⚠ The Planner wrote a licence question up as a confession, and the facts did not support it

**An entry recorded the HPA licence gap in the register of culpability** — *"converts an honest
mistake into a knowing one"* — **on a documentation shortfall.** ⚠ **The owner read it and asked
whether he was committing a crime. He is not, and it is not close.**

**Stated accurately, as `D-093 amendment 9` §5 now does: non-commercial academic display of
publicly-downloadable data, from a publisher that expressly encourages research use, source named,
licence quoted.** ⚠⚠ **An entry that OVERSTATES misleads exactly as much as one that understates, and
the next reader of that entry is a reviewer, a collaborator or an institution.**

⚠ **And the substance turned out smaller AND better-founded than the framing:** **`NA` fetched both
licence pages and found `v22` states BY-SA 3.0 while `www` states BY 4.0.** **Nobody misread
anything — HPA changed its licence between releases, and two accurate readings of two different
documents were reported as a dispute for four days.** ⚠⚠ **Resolvable by two HTTP requests the entire
time, and the Planner wrote an entry calling it irreducible.**

---

## §2 — ⚠ Four more Planner defects, all caught by Code

- ⚠⚠ **The wrong file was attached** — `D-093 amendment 2` sent as amendment 3. **The hash matched
  exactly.** **The contract proves bytes arrived intact; it cannot prove they are the bytes you meant
  to send.**
- ⚠ **`F-050` was pasted for a subject `RESERVED.md` had already reserved.** **The invariant went RED,
  correctly, and Code did not merge.**
- ⚠ **`F-053` was sent at `####`, the amendment level.** ⚠⚠ **Landing it there would have made it
  cited-but-not-defined — invisible to the very check the entry sits beside.** **Code landed it at
  `###` and asked to be overruled if wrong. It was not wrong.**
- ⚠⚠ **The Planner asserted `geom_proxy` was *"unbuilt and unrun"* in three documents**, while
  `F-017` records **run id=5 at 0.6607 / 0.6324 / 8-of-12**, twelve LOO percentiles, and a dedicated
  test file. **Unresolved; ordered.**

---

## §3 — ⚠⚠ What the day actually established, and it is a lot

**The licence is RESOLVED by measurement** (`D-093` amendment 8): **`v22` = BY-SA 3.0.** ⚠ **And the
version pin acquired a second function nobody predicted — `D-100` pinned v22 for comparability and it
pinned the LICENCE too.**

**Attribution went five-for-five, not two.** ⚠⚠ **`PA` found FIVE surfaces rendering HPA-derived data;
two were built THIS SESSION by the author of the convention, and the FIFTH was caught by the `PC3`
guard mid-report.** *"It fired before I'd finished writing the report claiming four."*

**`D-095` amendment 2** rules a fifth `tile_cut_kind`, **`unannotated_interior`** — ⚠ *the choice is
between an arbitrary cut that says so and an arbitrary cut wearing the name of a boundary.*

**`D-079` amendment ‹N›** rules the census split: ⚠⚠ **46.7% refused / 53.3% computable at the strict
bar — NEITHER branch amendment 1 committed to** — and rules the bar, the surface contract, and the
Planner's two pre-registration MISSES.

**`D-075 amendment 1`** adds a fourth confound — ⚠ **span selection, upstream of all three others:
*which sequence was folded at all.*** **39 of 82 corroborated, 31 never examined, 2 unreconciled and
KEPT.**

**⚠⚠ And the finding that reaches furthest: `F-053`.** **`CEILING_KNOWN_GOOD = 440` is a LENGTH; what
binds is ~1.26 GiB incremental against a 5.24 GiB RESIDENT MODEL.** **`preflight`, the climb and
`D-082`'s guard all measure the length axis.** ⚠ **Code's guard mis-calibrated three times by
reasoning about the length-shaped quantity instead of the memory-shaped one.**

---

## §4 — ⚠ PAE: a route, a fix, and no measurement

**`/api/analyses/{id}/pae` exists** — ⚠ **a route through the gate rather than production filesystem
access for an analysis question.** ⚠⚠ **A `404` is the ORDINARY case (2,692 of 2,771 carry none) so
the message names `F-042`, and a stored-but-missing path is a DIFFERENT 404** — *collapsing them
would hide data loss behind a known finding.*

**The `squeeze` defect was found only by running it** (`F-052` amendment 2), ⚠ **and the identical
bug one line above had already been patched without generalising to its sibling.**

**⚠⚠ `SA4`/`SA6` DID NOT RUN — 0 of 79 matrices were reachable, and Code declined to substitute the
20 `D-099` controls: *a different population, answering a different question in the right question's
words.*** **`P-006` is neither alive nor dead and the Planner's pre-registered expectation stands
untested.**

**Path (c) is ~7–8 h, local, overnight.** ⚠ **The 6.95 → 7.67 h move is RUN VARIANCE, not the fix** —
eight of ten folds agree within 5%, and the whole delta is the two longest folds under thermal
contention. **If a ruling is sensitive to that spread it needs repeat runs, not a better fit.**

---

## §5 — Open, and who holds it

| item | holder |
|---|---|
| ⚠⚠ **RENTAL — tomorrow's only subject** | Owner + Planner |
| ⚠ **`QC1` re-key `JA`** — nothing may be priced before it | Code |
| ⚠⚠ **One fold at ~500 aa, model released** — the cheapest datum on the board | Code |
| **`SA4`/`SA6`** — blocked on merging the read route | Code |
| **Merge `fix/pae-squeeze-and-read-route` @ `692e805`** | Owner |
| **`geom_proxy`: has it already run?** — ordered, unanswered | Code |
| ⚠ **`preflight` unwired** — and now known to guard the wrong axis | Code |
| **`F-042` path A/B/C** | Owner |
| **`P-006` registration**, gated on a literature search | Owner |
| **Backbone graphic spec**, and the census profile surface | Owner |
| ⚠ **The 3Dmol viewer defect — ordered 2026-08-19, still never reported** | Code |
| **Credential unrotated · backup present-not-verified** | Owner |

## §6 — ⚠ Habits that earned their keep

- ⚠⚠ **Stop when the first measurement refutes the premise the order rested on.** **0.03 GiB free
  of 8.0, and Code stopped rather than run nine more.**
- ⚠⚠ **Refuse the substitute population.** *The 20 controls would have answered a different question
  in the right question's words.*
- ⚠ **Ground the threshold in a measured success, not a fourth guess.**
- ⚠ **Select by HASH, not filename.** **The `(1)`-suffixed files were correct this time and wrong the
  last time.**
- ⚠⚠ **Report what a re-run does NOT establish.** *The move from 6.95 h is run variance, not the fix.*
