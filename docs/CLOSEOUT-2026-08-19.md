# CLOSEOUT — session 2026-08-19

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts) = `2058779229d88d04215435f92a338ecd7a798957538f383f0edfc8d1babb075b`
**bytes** = `10442`

> **State, and what is open.** The companion is `PREWORK-2026-08-20.md`, which is what to do next.
> ⚠ Where this file and the log disagree, **THE LOG GOVERNS.**
>
> ⚠⚠ **Eleven of `F-047`'s twenty-four members were made by the Planner, and ten of them today.**
> That is §1, and it leads. **A closeout that opens with the wins and files the failures at the back
> is a marketing document.**
>
> ⚠ **Naming:** log entries carry wall clock; documents carry the session name.

---

## §1 — ⚠⚠ The Planner corrected itself twice from doctrine, and the log had already ruled both halves

**Asked to unblock a production ingest, the Planner told the owner to hand-run an Alembic migration.
Then it *corrected* that — and the correction inverted the control.**

⚠⚠ **KEEL step 16: *hand-deploy dies. Merging is the only way to ship. Manual = emergencies only.***
**The Planner proposed making the owner the tired person at the terminal that branch protection was
built against.**

⚠⚠ **And the correction to THAT was also uninformed.** The log had ruled both phases already —
***"Phase 1 — the initial migration is run BY HAND, supervised, BEFORE the first deploy"***, and
phase 2, ***"a `release_command` … ruled but wired AFTER phase 1 succeeds."***
**The Planner was right, then wrong, then right again, and had read the entry at no point.**

⚠ **Root cause, and it produced four members today: reasoning from DOCTRINE instead of from the LOG.**
**Members 14** (asked a question `F-006` answered three weeks earlier), **17** (criticised a leak
`D-089` had already prevented), **19** and **20**. ⚠⚠ **Every one was preventable by one search, and
the search was cheaper than the paragraph that replaced it.**

**Answered by:** nothing structural. ⚠ **That is the honest state — the remedy is *search the log
before correcting from doctrine*, which is exactly the kind of rule that gets skipped under momentum,
which is when all four happened.**

---

## §2 — ⚠ Four more Planner defects, each caught by someone else

- ⚠⚠ **`F-014` was cited by CLASS, not INSTANCE.** The reserved row's description matched; its
  subject — `ecd_lengths.py`'s duplicated ceiling constants, already fixed and guarded — did not.
  **The Planner ordered Code to check the reservation's RELEASE CONDITION and asserted its SUBJECT** —
  ⚠ *it verified the half that had a check and asserted the half that did not.* **`F-044` amendment 1.**
- ⚠⚠ **`COMMITTED to docs/ as provenance` was FALSE on four documents.** **A document asserting its
  own provenance cannot be falsified from inside itself.** ⚠ Remedy: **the author writes
  `TO BE COMMITTED`; the landing header is stamped by whoever lands it.**
- ⚠ **The `## P-004` grep manufactures a false absence** — `⟡` in the heading means `^## P-00`
  excludes exactly the two entries the check is about. **A guard against a false collision producing
  a false absence.**
- ⚠⚠ **`F-050` was pasted for a subject `RESERVED.md` had already reserved for another.** **The
  invariant went RED, correctly, and Code did not merge.** ⚠ **The check caught a Planner error the
  same hour it was made. Owner ruling: the reservation wins.**

---

## §3 — ⚠⚠ `D-095` is RULED. The 776 are out from behind the design gate

**`D-095 amendment 1`** — twelve items — **moves `D-095` PROPOSED → RULED and discharges `D-091`
ruling 3.** ⚠ **Tranche 5's 776 rows are now held by ruling 2 — RENTAL SPEND — alone.**

**The document's headline was inverted:** ⚠ **125 tile at gaps · 10 have nothing to tile at · 6 need
one `run_interior` cut.** ⚠⚠ **And `merge_rule` was ruled a DECISION, not a parameter, because
overlapping-ONLY does not remove the cut — it RELABELS it**, filing a zero-residue sever through a
cadherin stack as a `gap` and extinguishing a `D-094` mount precondition while leaving the hazard.

⚠⚠ **And the amendment shipped with a hole the Planner did not see: 10 rows have NO LEGAL CUT SITE**
under the design's own vocabulary — no gap, no domain boundary, no run interior — while every one
exceeds the 1,026 trained context, so tiling is mandatory. **A category with a count and no ruling,
one week after the engulfing category was exactly that.**

---

## §4 — What was established, and it is substantial

**The clinical association layer, `D-093` amendments 2 and 3.** ⚠ Ships on two edges without survival;
`burden_supplier_unlicensed` a visible category. ⚠⚠ **`normal_tissue.tsv` has NO PATIENT COUNTS, so
the differential is NOT a subtraction** — co-equal display, and **the ratio pre-registered as not
computable from this supplier.** **Confirmed: 33 files in the v22 index, one IHC normal-tissue file,
everything else RNA — and substituting RNA would be the mRNA-laundering move `F-043` refuses.**

**⚠⚠ The licence exclusion named a column that does not exist.** `Cancer prognostics — … (TCGA)`:
**zero such columns in v22.** The real names are `prognostic - favorable` × 4 and
`Pathology prognostics - <cancer>` × 17. **A compliance guard passing since 2026-08-17 by matching a
string that never occurs.** ⚠ **And the page says ShareAlike 3.0 *International*, which is not a
licence that exists — UNRESOLVED, email drafted, host never captured.**

**`EE-0`, the most important item of the day**: `test_the_scorers_feature_path_is_closed` widened from
one field to every clinical-layer field, ⚠⚠ **including the RENAME route** — `mean_plddt_ecd →
mean_expression_ecd` reddens. **If expression reached the scorer, `P-001`'s question would stop being
answerable and nothing in the output would show it.**

**The authored-hash contract works — 4 of 4 byte-exact.** ⚠⚠ **And `core.autocrlf=true` would have
broken every one on a fresh clone** — a guard manufacturing its own corruption signal, **caught before
it fired**, `.gitattributes` added and proven by round-trip.

**The read-only role, proven at the database with a CONTROL** — three write verbs refused **and two
`SELECT`s returning 2,771**, ⚠ *because a role refusing everything would look identical.*
⚠⚠ **2,771 is the truncation's own number, read back through a role that cannot repeat it.**

**`FD1` closed without a fit.** Attributions are exactly `β_k·x̂_k`, all 56 rows to `1e-16`; **the
6×6 pairing tested, not inherited**; the intercept implied identically at `1.11e-15`.
⚠ **`F-051`: `membrane_proximal_plddt` 32.2%, `mean_plddt_ecd` 6.4% — a factor of five, and the
dominant confidence feature is REGIONAL, not global.**

**KEEL V9 is in the tree** — four documents, markdown, each carrying its source `.docx` `sha256`.
**Eleven principles, five named documents, Part C.** ⚠ Backup **present, not restorable-verified** —
6.1 h since last completed, 15.3 h since last full, artifacts volume snapshotted at 5-day retention.

---

## §5 — ⚠⚠ Three questions that could not be asked, and one that was asked wrongly

**`KB3` returned a THIRD state the pre-registration did not enumerate.** ⚠⚠ **Zero of 2,690 census
rows carry a `protein_features` row. The out-of-range count is UNDEFINED — the six-feature extraction
has never been run over the census.** **The Planner pre-registered two outcomes on a question whose
precondition it never verified.** ⚠ **So *score the census* is not a scoring task — it is a
feature-extraction job over 2,690 folds, and that is compute.**

**`KA3` was two paths that were one path written twice.** ⚠⚠ `sd(attr)/|r_k| == sd_raw(56)`
**identically, whatever `sd_k` is** — *the agreement is arithmetic, not evidence.* **Written and
refuted by its author before it reached the Planner.**

**⚠⚠ `JA6` was wrong in substance and the codebase had written the hazard down first.**
`protein_analyses.input_value` is **not unique** — 43 accessions carry two rows — and the join took
the census one. **The real predicate is `below_floor`: all eleven at `mean_plddt` 30.68–49.46.**
⚠ **`reads.py:167` already warns *"which row wins is whatever the database returns last —
nondeterministic, and silently so."*** **The production path is safe; the one-off script was not.**

**⚠ And a near-miss that never became a member:** a privilege sweep run with no `--database` landed in
Fly's `fly-db` sample tables and returned **a clean, correct answer about the wrong object.** **Had
the `TRUNCATE` proof run there it would have "proven" the reader safe against a database that is not
ours.** **Caught by reading table names.**

---

## §6 — Log entries this session

**`D-093` amendment 2 · `D-093` amendment 3 · `D-095` amendment 1 · `D-079` amendment 1 ·
`F-044` amendment 1 · `F-045` · `F-046` · `F-047` + amendment 1 · `F-048` · `F-049` + amendment 1.**
⚠ **Invariant at last report: 148 defined / 15 reserved / 164 cited, RED on `F-050`** — resolved by
landing the reissued finding at its corrected integer.

---

## §7 — Open, and who holds it

| item | holder |
|---|---|
| ⚠⚠ **Rental tranches — tomorrow's subject** | Owner + Planner |
| ⚠⚠ **Census feature extraction — compute, competing with rental for the same budget** | Owner |
| **`ED` ingest** — `D-100` bar inside the transaction | Owner + Code |
| **`EE` surface** — per-datum HPA deep links, gated on `ED` | Code |
| ⚠ **`D-095` amendment 2** — the 10 rows with no legal cut site | Planner |
| **`F-047` amendment 2** — members 22, 23, 24 | Planner |
| ⚠ **`F-049`'s third instance** — `ranked` 67 vs 56, neither endpoint stating its population | Code |
| **The guard-direction sweep** — `F-050`, still unwritten | Planner |
| ⚠ **`preflight()` unwired** — blocks the ceiling climb | Code |
| ⚠⚠ **The 2026-08-17 credential, unrotated** — owner's standing decision, recorded | Owner |
| **The HPA email** — deferred, knowingly | Owner |
| ⚠ **Backup: present, not restorable-verified; `gjpkdonnmkeoyln4` deleted** | Owner |

---

## §8 — Habits that earned their keep

- ⚠⚠ **A control case in every proof.** Three write verbs refusing looks identical to a role that
  refuses everything. **Two `SELECT`s returning 2,771 is what made it a measurement.**
- ⚠⚠ **Report whether each thing EXISTS before implementing it.** `D-093`'s five gate assertions had
  never been written. **Had the order said *implement these*, nobody would have learned it.**
- ⚠ **The set difference, never the arithmetic.** `40` vs `56` was a **net figure hiding a 10-out /
  26-in exchange.** A sentence would have been wrong in both directions.
- ⚠⚠ **Test the pairing, don't inherit it.** 6×6, diagonal `1e-16`, off-diagonal `1e-1`.
- ⚠ **A declared BYTE COUNT beside every hash.** A stated length caught a corruption two truncated
  checksums could not.
- ⚠⚠ **Name the target explicitly; never accept a default.** `--database`, and the missing
  `straddle` `TypeError`.
