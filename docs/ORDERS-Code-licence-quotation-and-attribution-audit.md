# ORDERS — Code — which page, the quotation, and whether per-datum attribution actually shipped

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts) = `b0d10cc79f96fe981a5853fb3cb4d85c3f071e78c47458ab1261e49ad48e0652`
**bytes** = `5645`

> ⚠ **DOWNLOAD AND COMMIT. DO NOT RETYPE. No landing header.**
> ⚠ **SUPPLEMENT to `ORDERS-Code-invariant-amendment-references.md`.** That order's `MC` said *land
> the 2026-08-19 amendment 3*. ⚠⚠ **THAT IS CANCELLED — the paste has been REISSUED** because
> amendment 4 shipped the surface it ruled on. **`MC` now means: land the REISSUE.**
>
> ⚠ Grounding is the snapshot at `b06d378`, **`git archive`, no manifest, dirty state unknowable.**
> **No GPU, no rental, no fold, no fit.**

---

## §0 — Owner rulings carried in (2026-08-20)

**R1 — The surface QUOTES the page.** *"Say what the page says. We are grounding the source so that it
is verifiable, and we're not claiming that information as our own."* ⚠⚠ **This supersedes amendment
4's fix in the direction of MORE information: amendment 4 correctly removed a false assertion and
left silence; the ruling replaces silence with attributed quotation.**

**R2 — Land the reissued `D-093 amendment 3`.**

**R3 — Verify per-datum attribution actually shipped with amendment 4's panels.**

⚠ **R4 — the HPA email is deferred, knowingly**, and the reissue records the identity as unresolved
rather than resolved by inheritance.

---

## §1 — ⚠⚠ Task NA — WHICH PAGE. R1 is unexecutable until this closes

**`docs/HPA-licence-2026-08-19-as-read.md` records the host as `NOT CAPTURED`.** ⚠ **A quotation
cannot be *"grounded so that it is verifiable"* if we cannot name the URL a reader would open.**

**NA1 — Fetch BOTH, same session, and report status, final URL after any redirect, and `sha256` of
the body:** `https://v22.proteinatlas.org/about/licence` · `https://www.proteinatlas.org/about/licence`.

**NA2 — ⚠⚠ DIFF THEM.** **If they differ, the v22 text governs our ingest and the www text does not,
and the as-read document may be a record of the WRONG PAGE.** **Report the diff, not a verdict.**

**NA3 — ⚠ If `v22` REDIRECTS to the current release, that is itself a finding** — it would mean v22's
terms are not independently retrievable, **and it refutes `D-093` decision 6 question 4's stability
answer, which rests on *the version is in the host name*.** **STOP AND REPORT.**

**NA4 — ⚠ Diff whichever page governs against the as-read verbatim region**
(`4ac225892312f56c6843ea16a7da7c1bcdd798bfda48b088badd15b7b583b13d`, 4,177 bytes). **Two independent
readings compared on the bytes.** ⚠⚠ **If they differ, the page changed between 08-19 and today and
a NEW dated as-read file records that — the existing one is never updated.**

## §2 — Task NB — the quotation, and every part is a condition

**NB1 — ⚠⚠ REPORTED SPEECH.** *"The Human Protein Atlas states, on its Licence & Citation page (read
‹date›):"* then the quotation. ⚠ **Never *"this data is licensed under…"*** — **that is adoption, and
adoption is what the ruling refuses.**

**NB2 — ⚠ VERBATIM, including *"Attribution-ShareAlike 3.0 International"*.** **The surface does NOT
note that this licence does not exist.** ⚠⚠ **That observation belongs in the log, where it is, and
correcting someone else's page on our page is worse than quoting it.**

**NB3 — The link and the date read render WITH the quotation**, using `NA`'s determined URL. ⚠ **A
quotation without a resolvable link is not grounded; it is just a claim in someone else's words.**

**NB4 — ⚠ Prove it RED**: remove the link, or the date, or the attributive clause, and the suite
fails. **Three separate assertions, not one** — *each asserted property gets its own test*,
KEEL-1 V9 Principle 6.

**NB5 — ⚠⚠ Report the rendered text verbatim in your reply, and WALK the page.** *UI honesty gaps are
found by walking, not only by tests* — **and the 3Dmol defect is still the standing proof, still
unreported.**

## §3 — ⚠⚠ Task NC — did per-datum attribution actually ship?

**`D-093` amendment 4 shipped the cancer connection on both card types. Amendment 3 item 3 requires a
DIRECT LINK PER RENDERED DATUM to `v22.proteinatlas.org`, plus a primary publication, the website
reference, and the image/data credit.** ⚠ **The Planner cannot tell from `b06d378` whether that
shipped. This is the actual compliance question and it is ours alone — no reply from HPA changes it.**

**NC1 — For a protein with a tumour edge and one with a normal-tissue edge, report what renders**:
does **each value** carry its own `v22` deep link, or is there a single block per page?
**NC2 — ⚠ Report the primary publication actually cited.** **If it is the 2017 pathology-atlas
transcriptome paper, that is the wrong modality** — amendment 3 item 7.
**NC3 — ⚠ Is the image/data credit *"Human Protein Atlas"* present as its own element?**
**Amendment 1 clause 3 dropped it, so it may never have been built.**
**NC4 — ⚠⚠ If per-datum linking did NOT ship, say so plainly and do not fix it in the same message.**
**Report first.** **The scope of the gap decides whether this is a rendering change or a data-model
one, and that is a ruling.**

## §4 — Task ND — land the reissue

**`D-093-amendment-3-REISSUE.md`.** ⚠ **Placed in ORDINAL POSITION between amendments 2 and 4**, not
appended after 6. **Report the byte range you inserted and assert it matches the declared hash.**

⚠ **Then run the amendment-aware invariant from the other order and report it GREEN**, with the
before-and-after outputs — **the red case was `D-093 amendment 3` and this is what clears it.**

## §5 — ⚠ Not ordered

**No email.** **No ingest, no migration, no fold, no rental, no credential work.**
**No change to `D-075` or `geom_proxy`.**
⚠⚠ **And do not resolve any dangling reference by deleting a citation. A citation removed is a
finding erased.**

## §6 — Report

⚠ **`NA`'s two fetches and the diff FIRST** — `NB` cannot start without a URL.
Then `NC`'s answer as observed renders, not as code reading · the quotation verbatim · branch and tip
· **number and title of every entry landed in the message that lands it** · the invariant with its
keys **including the amendment figures** · the gate without `.env` sourced.
