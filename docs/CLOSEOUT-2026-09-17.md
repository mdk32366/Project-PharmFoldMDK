# CLOSE-OUT — 2026-09-17 · R1–R4 read, `D-168` written, Phase 1 measured, `P2` closed, two instruments built

**Owner:** Matt Kelly · **Planner:** Claude Opus 5 · **Builder:** Code
**Session date:** ⚠ **2026-09-17, America/Los_Angeles (PDT, UTC−7)** — opened **10:21 PDT /
17:21 UTC**, measured (`Get-Date`). ⚠⚠ **The `09-17` labels are the real calendar date**, unlike the
2026-09-15 session that ran under documents labelled 09-16 (`CLOSEOUT-2026-09-16` §6).
**Base:** `origin/main` @ **`1e67954`** (the #331 merge). ⚠ **Not `f7d0a9d`** — Code's prework named
its own parent while `main` had moved twice. Restated at session open, and again here.
**Branch:** `d167-r1r4-population-read` → PR **#332**.
**Governs:** `ORDERS-Code-2026-09-17-R1-R4` + AMENDMENTS 1 & 2 · the post-tunnel, TASK-D, tunnel-2,
offline and reconciliation orders · `RULING-2026-09-16` · `RULING-Owner-2026-09-17`.

**Next free integers:** **`D-169`** · **`F-083`** · **`A-032`** (`docs/RESERVED.md`).
⚠ **Every spend moved its pointer in the same commit.**

---

## 1. What landed, each claim with its artifact

| # | what | evidence |
|---|---|---|
| 1 | **R1–R4 read** — the population-aware read the Phase E report could not take | `data/control/d167/population_read/r1r4_read.json`, sha256 `aff81593…e15935`; `r1-tunnel-open.txt`, `r2-read-console.txt`, `r3-tunnel-closed.txt` |
| 2 | **`D-168` WRITTEN** — assembled features stored tagged, never pooled | `docs/decisions.md:19`; commit `8005f27` |
| 3 | **`F-081` + `F-082` WRITTEN**, `F-` pointer 081 → 083 | `docs/findings.md:19`, `:82`; commit `222bcdb` |
| 4 | **E1 · E2 · E3** — the three enumerations | `data/control/d167/enumerations/e1_e3_read.json`, sha256 `6c1c634e…4cc32` |
| 5 | **Phase 1 `C5`**, read alone before `C1` | `data/control/coverage/c5_read.json`, sha256 `0ff35dbd…2b8cb` |
| 6 | **Phase 1 `C1a`/`C1b`/`C1c`** | `c1_read.json`, sha256 `2afed323…20d680` |
| 7 | **Phase 1 `C2`/`C3`/`C4`** | `c2_c3_c4_read.json`, sha256 `f10883cd…f43095` |
| 8 | **Four instruments**, all tests-first | `d167_population_read.py` · `feature_coverage_report.py` · `d167_enumerations.py` · `pdb_coverage_census.py` |
| 9 | **Every governing document landed in `docs/`** | the delivery block has **no open instance** |

**Each artifact re-hashes on disk after its tunnel closed, and each is EOL-protected BY NAME** in
`.gitattributes`. ⚠ **The glob is not enough for a byte-for-byte guard.** **No artifact was ever
deleted, rewritten, or re-run.**

---

## 2. The readings, each with its key

| reading | expected | measured | |
|---|---|---|---|
| **R1** groups of `(accession, tile_start, tile_end)` with > 1 complete run-1 row | 72 | **72** | MET — list complete at 72 shown, `{2: 72}` |
| **R2** each holding exactly one tranche-0 and one tranche ≥ 1 row | 72 of 72 | **72 of 72** | MET |
| **R3** identity key **with** population | 0 | **0** | MET — and see §3 |
| **R4** three named accessions: no complete tranche-0 row, exactly one complete census row | 3 of 3 | ⚠ **2** | **NOT MET → `F-082`** |
| **C5** v1 ids present in `protein_features` | — | **2,690 of 2,690**, sample 50/50 equal | v1 **did** reach the table |
| **C1a** representatives with no feature row | ≥ 773 | **776** | at or above its floor |
| **C1b** of those, already refusing as assembled | — | **45** | extraction changes nothing visible |
| **C1c** = C1a − C1b | — | ⚠⚠ **731** | **the ONLY true coverage gap** |
| **C2** stale representatives | none pre-set | **0** | |
| **C3** cohort rows with no feature row | — | **0** | ⚠ **this closes `P2`** |
| **C4** C1a by `structure_kind` | — | assembled **45** · **mucin 3** · single-pass **728** · tiles_only **0** | sums to 776 |
| **E3** cohort accessions lacking a complete tranche-0 row | `P11717`, `Q8WXI7`, `Q9NYQ8` | **exactly those three** | stop did **not** fire |

### 2.1 ⚠⚠ `P2` is CLOSED — OUT, on a measurement

`C3 = 0`. The owner's population ruling made P2 conditional on `C3` showing a gap; **there is no
gap**, so the condition never fires. **P1 in · P2 OUT · P3 a ≤100 tagged instrument sample · P4 out.**
⚠ **The last open owner decision from this morning's list, closed by a reading rather than a
judgement.**

### 2.2 The coherence check — corroboration, not a finding

**3,466 − 776 = 2,690 = v1's ids = the rows in the table, with `C2 = 0`.** The covered set **is**
v1's set; **there is no unaccounted source**, which is the concern the `C1a` floor existed to catch.
⚠ **Bounded:** this is about **census representatives**, not the whole table — `C3 = 0` proves the
cohort rows carry features too. **Two populations, two accountings.** ⚠ **No integer; not a finding.**

⚠ **`C1a` = 776 against the void file-based 777 is a FOOTNOTE.** The source CSV is gone; nothing
reconciles against it and the difference was not computed.

---

## 3. `F-081` — real in source, zero instances in the database

`core/hold48.py`'s `emit_tile_jobs` writes **no `run` key**, so a tile emitted **after**
`backfill_run_label.py` declares no generation and is invisible to every reading that selects Run 1 —
⚠ **including `app.reads.keep_run_1`, which is what the census surfaces select by.**

**Measured:** `(absent)` reads **whole_protein 0 · tile 0 · partial_tile_keys 0**.
⚠⚠ **That is what makes `R3 = 0` a measurement rather than an artifact of the predicate**: had an
unlabelled complete whole-protein row existed, it could have sat inside an R1 group and been invisible
to both readings. **The Planner pre-registered that discriminator as a stop; it did not fire.**

**Latent, not active. It fires on the next tile emission.**

---

## 4. `F-082` — R4's expectation was mis-specified, and the database is sound

`Q8WXI7` (MUC16) holds **no complete run-1 row of any kind** — 0 · 0 · 0 · 0 — and appears in **none**
of the 72 R1 groups. **It satisfies R4's first clause and fails its second by holding none, not too
many.**

**The cause:** A9.2's *"three named overlap exceptions"* conflated **a planned tiled rental span**
with **a protein actually folded as tiles.** `P11717` and `Q9NYQ8` are both; `Q8WXI7` is only the
first. ⚠ **The committed record already said so** — `decisions.md:2860` (`structure_kind: mucin`,
`folded: false`), `:3041` (live `NOT FOLDED`), `:6883` (the mucins stay `out_of_class`).

⚠ **`R1 = 72` STANDS** as a direct measurement and **does not inherit** the Phase E derivation. The
`75 − 3` subtraction was **arithmetically right for the wrong reason.**
⚠⚠ **The "75 overlap" figure rests on the same conflation — re-derived today from the committed files
(75, with the 7 cohort-only accessions named) and re-derived before any future citation.**

**Sub-question (a): CLOSED** — §5 below. **Sub-question (b): OPEN.** `C4`'s `mucin = 3` and `E2`'s
three pending accessions are the **same three proteins from two independent readings** — ⚠ **evidence
toward a class property of mucins, never an answer to it.**

---

## 5. ⚠⚠ TASK N — the MUC16 row count, reconciled from one artifact

**Two of Code's own documents disagreed: one row, or two.** Reconciled from `e1_e3_read.json` alone,
no tunnel:

- ⚠ **`E1` is right. MUC16 holds exactly ONE row in the database** — job **3073**, `pending`,
  whole-protein, run `1`, **tranche 5**. `E1` filters nothing, so `total: 1` is every job row that
  exists for that accession.
- **The other figure described a TEST FIXTURE**, where `_seed` deliberately gives MUC16 a row on each
  side so CI can prove the instrument reports **rows** and **accessions** separately. ⚠ **The fixture
  is doing its job; the report should not have quoted its shape as a measurement.**
- **In production the two are equal:** 5 non-complete rows → **5 distinct accessions**. ⚠ **A correct
  total (5) carried a wrong cause**, which is how it survived being written down.
- ⚠ **"No cohort-side row at all" SURVIVES, and is now the stronger claim:** the only tranche-0 row
  among the five is `P11717` (job 57, failed). **Boundary:** whether a `protein_analyses` row exists
  for MUC16 **without a job** was not measured by either reading — that is the cohort-account query
  ordered for the next tunnel, and FAT2 raises the same question.
- **`F-082` needs NO amendment:** it asserts only **complete**-row counts, all still correct, and the
  one row is `pending`.

---

## 6. Method and instruments

### 6.1 ⚠⚠ CI was the instrument for three of the five defects Code produced

| # | defect | caught by |
|---|---|---|
| 1 | a count taken from a **list length** (`len(R4_ACCESSIONS)`) | Code's own test, before any read |
| 2 | **printed output not ASCII** — `core.db_role.format_preamble`'s section sign | ⚠ **CI only** |
| 3 | a **`C4` branch vanished at zero** instead of reading `0` | ⚠ **CI only** |
| 4 | **rows and accessions conflated** in a test assertion | ⚠ **CI only** |
| 5 | ⚠ **a test fixture's shape reported as a production measurement** | ⚠⚠ **the Planner, reading two of Code's documents against each other** — §5 |

⚠⚠ **All three CI-only defects were invisible locally because the local machine has no database.**
**Record this as a standing fact about the project's test topology, not as a run of bad luck:** the
pure half runs everywhere, the half that touches data runs only in CI, and **anything the fixtures
alone can prove is proven late.**

### 6.2 The reusable method note

⚠⚠ ***A source-only ASCII check is not an output ASCII check.*** Only the assertion on the **printed
bytes** caught the section sign, and only in CI. **This is a LESSON, not a changelog line.**
⚠ `core/db_role.py`'s section sign stays on the existing "make printed output ASCII" item — **a
shared module is not a read-day edit.**

### 6.3 ⚠ One root cause, three instances, both parties, one day

**A count stated without the population it counts over.**

| instance | shape |
|---|---|
| Planner error 12 | R4's set built from the manifest without checking each element's fold state |
| Planner error 15 | `3,467 − 82`, valid only if the cohort were a subset — **75 of 82 are** |
| Code's fifth (§5) | a **fixture's** population quoted as **production's** |

**Named once, in those terms**, because the three look unrelated and are not.

### 6.4 The instruments
Four, all **tests-first, RED at the assertion**, all **CI-green before reading anything live**:
`d167_population_read.py` (R1–R4) · `feature_coverage_report.py` (C1–C5, modelling the profile
short-circuit, with `--sections` so the `C5` pause computes nothing else) · `d167_enumerations.py`
(E1–E3, with E3's stop shown firing) · `pdb_coverage_census.py` (PDB coverage **T0**, offline, no live
client can exist). ⚠ **The last two have never been run against live data at all.**

---

## 7. The tunnel account — TWO tunnels, both fully evidenced, no third

| # | opened | closed | reads |
|---|---|---|---|
| **1** | 20:51Z | 20:52:45Z, **0 listeners** at 20:52:51Z and again at 21:23:51Z | R1–R4 |
| **2** | 22:56:48Z | 22:58:32Z, **0 listeners** after | E1–E3 · C5 · C1 · C2/C3/C4 |

Each carries its **own** open paste, **own** Direct-IP corroboration (`fdaa:62:76d9:0:1::9`), **own**
single-listener count and **own** close paste. ⚠ **Tunnel 2 was never merged into tunnel 1's
account.**

⚠ **`AMENDMENT 2` §4's "tunnel 2 declared" is VOID for the morning** — case (i) stands, established
from process evidence, with **`flyctl agent run` (pid 14960)** named as the likely source of the
impression: long-lived, predates the tunnel, **holds no listener.**

⚠⚠ **The owner-at-the-keyboard departure on BOTH reads is recorded, not smoothed.** The orders say
*owner at the keyboard*; the owner directed Code to run them. **Authorisation held in substance; the
departure is written into both tunnel-open files.**

**Step-0 probe read `10003`, exit 0, both times.** ⚠ Code's tool injects `PYTHONIOENCODING`, so the
probe is weaker evidence here than in the owner's terminal — **named, not glossed.** Both instruments
escape their own output to ASCII regardless.

---

## 8. `C4`'s fourth branch, and SPEC v2's eighth outcome

- ⚠ **`C4` has FOUR branches, not three.** `choose_census_representative` returns `assembled`,
  `tiles_only`, `single-pass` **and `mucin`** (`app/reads.py:980–997`). **Folding `mucin` into
  `single-pass` would be `D-168` §3's defect**, and it is not academic: **`mucin` is MUC16's case.**
  **Planner error 14**, accepted.
- ⚠⚠ **SPEC v2 gains an EIGHTH outcome: `overlap_identity_below_minimum`** — an entry that overlaps
  well but fails the 95% identity floor had no home in the seven. Folding it into
  `overlap_below_minimum` would report an **identity** problem as a **length** problem; into
  `no_pdb_entry`, **our filter as nature's absence.** **Recorded here as a Planner amendment to the
  spec's §1; the committed spec's body is NOT edited.** ⚠ **T4's sum checks are against eight.**

---

## 9. The folded-flag provenance (TASK M) — a reading; nothing was changed

`folded` is set by `apply_structure_kind` (`app/reads.py:1001`) from the picker's **kind**; the
surface prints `NOT FOLDED` on `folded === false` (`ui/src/structureStatus.js:63`, `:83`).

⚠⚠ **A `pending` row CAN produce `NOT FOLDED`, and the path never reads `jobs.status`.** The mucin
branch is reached by **accession identity** (`core/hold48.py:51`, a frozenset of the three) **plus the
absence of any `pdb_path`.** **A pending row, a failed row, and no row at all all produce the same
label.**

**So the source supports the first reading: the label is the honest permanent state, and the pending
rows are the stale artifact** — ⚠ **except that one string makes a queue claim the rows contradict:**
*"mucin — out of class; never ESMFold (D-111). **Rental is closed (pod Terminated)**"*.

⚠ **The tension is in the SENTENCE, not the flag. A copy question, the owner's call, no code.**
⚠ **Retiring those three rows would change NO surface** — nothing in the path reads job status — so
the two candidate fixes are **not symmetric in cost.** **Nothing was changed.**

---

## 10. Error accounting

| party | count |
|---|---|
| **Planner** | **15** — 9 (a second name for one refusal) · 11 (documents cited but undelivered) · 12 (R4's set built without checking fold state) · 13 (a Builder state asserted without verification) · 14 (`C4`'s three-branch vocabulary) · 15 (the `3,385` subtraction) |
| **Code** | ⚠ **8** |

⚠⚠ **Code is 8, not 7, and the difference is deliberate.** The orders proposed 7 on the ground that
the four defects were *"caught before they reached a reading"* — **true of those four, and not of the
fifth.** §5's defect reached **two documents** and would have reached this close-out. **An instrument
catching its author is the system working; a reader catching it afterwards is not the same thing, and
Code does not keep the flattering count.**

---

## 11. Owed, carried forward

### 11.1 Owner decisions still open
- **The mucin copy's queue claim** (§9) — retire the rows, or stop the sentence describing the queue.
- The paper's **§4.3 loss statement**, now zero rows — owner's wording.
- **v2 timeline class dates** — the proposal PDF is rebuilt around them.
- ⚠ **Credential rotation — deferred deliberately.** `.env` still carries a credential on the dead
  **16380**. **The literal-secret scan is the only guard and the repository is public.**
- **`D-169` / `D-170`**, owed to `SPEC-A-032` §2.6 / §3.7, after `D-168`.

### 11.2 Next sitting, in order
1. **The cohort-account query** — what the three missing accessions hold **instead**. ⚠ FAT2 has no
   complete cohort row **and** no non-complete run-1 row; §5's boundary asks the same of MUC16.
2. **`A-032` step 1** — pass criterion pre-registered **before** the pull.
3. **T1** of the PDB census. ⚠ **Its stop condition needs a fresh head.**
4. **Phase 2 extraction** — unblocked by `D-168`, **owner present**, its own orders. ⚠ **Ingest still
   barred** pending the §2.3 citation list and `census_ingest_features.py` carrying `D-159` + the role
   preamble.

⚠ **The next sitting grounds on the MERGE COMMIT**, not on `1e67954`.

### 11.3 Standing, recorded, not repaired
`F-079`'s three open exceptions · the citation-invariant residual · the **four stale `RESERVED.md`
rows** (`F-073`, `A-031`, `D-158`, `D-156`), ⚠ **not struck** without checking the `re.search` pins ·
`core/db_role.py`'s section sign · ⚠ the untracked `_tmp_c1_*` / `_tmp_c2_*` helpers — **not read, not
touched, not ruled on**, and **none landed.**

---

## 12. Hygiene, as executed
Explicit paths on **every** `git add` · credential scan before **every** commit: literal password
absent, 0 credentialed URLs · evidence LF and ASCII · printed output ASCII **on the printed bytes** ·
artifacts EOL-protected **by name** · **nothing deployed; nothing in PR #332 is deployable.**
