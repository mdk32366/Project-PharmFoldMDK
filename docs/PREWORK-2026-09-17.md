# PREWORK — 2026-09-17 (next session) · The population-aware read, then feature coverage

> **Read this before anything else.** It is written at the close of the 2026-09-15 PDT session and
> grounded on **`main` @ `f7d0a9d`** (the #330 merge), Fly **v237**.
> **Next free:** **`D-168`** · **`F-081`** · **`A-032`** — verify against `docs/RESERVED.md` before
> spending one.
>
> ⚠ **This document states its own date frame.** It is written on **2026-09-15 PDT** for the next
> session. The 09-16/09-17 labels are calendar labels, not measurements (`CLOSEOUT-2026-09-16.md` §5.5).

---

## 1. Where things stand

**Closed yesterday:** the 37 are re-attached (`D-167`), the three duplicate tiles collapsed, `0014`
applied, the debt paid and its reminder deleted. Witness **480 → 517**. Evidence under
`data/control/d167/`, EOL-protected and hash-verified.

**Not closed, and the order matters:**
1. **R1–R4** — the population-aware read (§2). ⚠ **First item. Nothing else touches the database before it.**
2. **Feature coverage** — `ORDERS-Code-2026-09-16-feature-coverage.md`, whose Phase 1 shares R1–R4's single
   tunnel (§3).
3. **`F-079`'s three temporary exceptions**, one of which (`census_ingest_features.py`) blocks that work's
   Phase 3 (§4).

---

## 2. ⚠⚠ FIRST: R1–R4, one read-only tunnel

Carried **verbatim** from `ORDERS-…-reattach-and-collapse` A9.2:

| reading | expectation |
|---|---|
| **R1** | groups of `(accession, tile_start, tile_end)` with > 1 complete `run '1'` row, **its own `count(*)`** | **72** |
| **R2** | each such group holds exactly **one** `cohort_tranche = 0` row and exactly **one** tranche ≥ 1 row | **72 of 72** |
| **R3** | the identity key **with** population: groups with > 1 complete row | **0** |
| **R4** | the three named exceptions (`P11717`, `Q8WXI7`/MUC16, `Q9NYQ8`/FAT2) have no complete tranche-0 row and exactly one complete census row | **3 of 3** |

- **Any miss is a finding.** ⚠ **R3 ≠ 0 is a genuine same-population duplicate**, and it bears on the Run-2
  identity design that blocks slice 4 — nothing indexes whole-protein identity.
- **Confirm the tranche convention from `db/models.py` and the enqueue scripts first**, never from the
  amendment.
- **Build on `scripts/d167_phase_e_read.py`**: tests first, `SET TRANSACTION READ ONLY`, role preamble,
  `D-159`, write-once output with its sha256, ASCII output.
- ⚠ **Fix the defect that instrument already has:** every count reads its own `count(*)`, and any capped
  list is labelled capped. A capped list must never be printed as a count.

---

## 3. Feature coverage — what Code verified before the session opened

`ORDERS-Code-2026-09-16-feature-coverage.md` was analysed at `f7d0a9d`. **Its §1 reproduces exactly**, and
three things it asks for are already true or already exist.

### 3.1 §1 re-derived from the committed files ✅
Recomputed from `feature_coverage_census_v7_file_based.csv` (sha256 `f9104d4e…d50c4`, 3,467 rows):

| status | count |
|---|---|
| `profile_renders` | **1,397** |
| `out_of_support` | **1,225** |
| `no_feature_row` | **777** |
| `refused_span_below_floor` | **58** |
| `features_incomplete` | **10** (all: *radius_of_gyration: fewer than two CA atoms*) |

The 777: tranche 5 ×776 + `P55073`; rental 776 / local 1; surface 660 / non-surface 117; spans 237–14,451,
median 662; bins ≤630 **352**, 631–1,026 **284**, 1,027–1,652 **93**, >1,652 **48**. Binding features:
`mean_plddt_ecd` 868, `membrane_proximal_plddt` 484, `sasa_normalized` 201, `ecd_length` 146,
`radius_of_gyration` 142, `largest_patch_fraction` 18. **Every figure matches the orders.**
⚠ Still to do as ordered: `scripts/feature_coverage_report.py`, **tests first**, so the check lives in the
tree rather than in this document. The CSV is a check on that script, never its input.

### 3.2 §2.4.1 — the instrument is the same ✅ (measured, not assumed)
- `feature_version()` reads **`e67a8cf30c1c`**; the v1 manifest pins the same.
- `git log 8db7345..HEAD -- core/features.py` is **empty**: unchanged since the v1 extraction.
⇒ v2 would be the same instrument. **The stop condition in §2.4.1 does not fire.**

### 3.3 §3's pre-C5 question — answered from source ✅
**The profile panel reads the `protein_features` TABLE, not the artifact.** `census_profile_statuses`
outer-joins `ProteinFeatures` (`app/census_profile_read.py:119–124`) and `census_profile_block` does the
same. The v1 manifest records `wrote_database_rows: false`, so whether v1 ever reached the table is exactly
what **C5** must measure.

### 3.4 ⚠⚠ §2.2 asks for a category that ALREADY EXISTS — raise before writing `D-168`
The orders propose `refused_assembled_structure`. The tree already ships
**`refused_assembled_incommensurable`**:
- declared `core/structural_profile.py:53`, returned at `:270`;
- applied `app/census_profile_read.py:128` via `_incommensurable_assembly` (`:83`);
- rendered `ui/src/components/CensusTable.jsx:212` — *"assembly — not a single-pass measurement"*;
- pinned by `test_structural_profile.py`, `test_census_assembly_review.py`, `test_d134_stitched_parent_identity.py`, `StructuralProfile.test.jsx`;
- and it is `D-120`'s ruling, recorded at `docs/decisions.md:13588`.

⚠ **Two names for one refusal is `F-049`'s family.** `D-168` should **reuse the existing category** and
record why, or state what the new one measures that the old one does not. **Ask the Planner before
writing the entry.**

### 3.5 §2.3 — the citation sweep, done
`census_features.v1` / `c08f9f1d` are cited in **7 documents**: `decisions.md`, `findings.md`,
`D-079-amendment-6-paste-ready.md`, `CLOSEOUT-Code-2026-08-19-census-to-profile.md`,
`MEASUREMENT-OUTPUT-2026-08-20-structural-profile.txt`, `ORDERS-Code-census-feature-extraction-overnight.md`,
`MEASUREMENT-OUTPUT-2026-08-19-census-extraction-and-KB.txt`.
⇒ **v2 is additive.** Every sealed analysis keeps citing v1 by sha256; replacing it needs a ruling.

### 3.6 Decisions the owner still owes before extraction
- **§2.1 the population:** P1 in; P2 (cohort 82) in if C3 shows a gap; P3 Run-2 rows as a ≤100 instrument
  sample only; P4 tiles out.
- **§2.2 `D-168`**, with §3.4's correction first.
- **§2.3** the pins, reviewed against the seven files above.

---

## 4. Order of work

1. **R1–R4** (§2), one read-only tunnel, owner at the keyboard. Report before anything else.
2. **Feature coverage Phase 1 (C1–C5)** in the **same** tunnel — extend the R1–R4 script.
3. `scripts/feature_coverage_report.py`, tests first, output equal to §3.1.
4. **`D-168`** written after §3.4 is ruled; extraction to an artifact may proceed, **ingest may not**.
5. **Phase 2 extraction v2**: `--out`/`--manifest` overrides, refusal to overwrite `v1*`, the new
   categories, the timeout, the v1-equality comparator, the bracketing run on the 10 largest **reported
   before** the full pass.
6. **Phase 3 ingest** only after `D-168`, the citation review, and `census_ingest_features.py` gaining
   `D-159` + the role preamble (which also narrows `F-079`).

## 5. Standing conditions for any tunnel day
- **Step 0 is the encoding fix and its probe:** `$env:PYTHONUTF8 = "1"`, `[Console]::OutputEncoding =
  [Text.UTF8Encoding]::new($false)`, then the probe must read **`10003`** with exit 0. Both lines, or the
  logs take mojibake silently.
- **`.\.venv\Scripts\python.exe` everywhere**, including `-m alembic`. The PATH python is 3.14 with
  different pins.
- **Tunnel bound by cluster name**, Direct IP corroborated against `fly mpg status`, one tunnel, closed
  after, and both pasted.
- ⚠ **`.env` still points at the dead port 16380.** `$url` takes the credential from it and names 16391
  explicitly. If the owner has rotated the credentials (A8.6), `.env` may no longer carry one at all —
  **check before building `$url`.**
- Every log file is scanned before its commit: literal secrets stop it; bare words are listed (A10.3).

## 6. Out of scope, named so none of it happens in passing
Slice 4 · refitting or re-standardising the model · widening the support · scoring census rows
(`D-079` dec 1) · re-folding anything · PAE-dependent features (`F-042`) · `core/scorer.py`.
