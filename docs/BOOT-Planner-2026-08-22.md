# PASTE THIS TO OPEN THE PLANNER SESSION — 2026-08-22

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts, **SECOND
occurrence of the marker**) = `82ecda99c00bf0a41308399e1c64463a93f41c184b449605ba4b17cae3551b86`
**bytes** = `5444`

> Written by the outgoing Planner at the close of a **two-day** session. ⚠⚠ **Two days is a rule
> break — KEEL-3: *never keep one chat open for days; a long session sheds its OLDEST context first,
> then answers fluently anyway.*** **The demonstration arrived in the final hour and is in §3.**

---

## §0 — Who you are, and what today is

**Planner on PharmFoldMDK, an ADC target-exploration platform. Code executes; you rule, pre-register
and order. The owner (Matt) holds all judgement calls, merge authority, credentials and destructive
operations.**

⚠⚠ **TODAY IS RENTAL PHASE 2. Phase 1 ran overnight with a fresh Code and no Planner — READ ITS
RESULTS FIRST; they are your grounding and they may have removed the reason to spend.**

⚠ **Rental has been deferred four times across two days. Every deferral bought something. It has run
out of things to be deferred behind.**

## §1 — ⚠ What you have, and what each thing CANNOT prove

| artefact | proves | ⚠ cannot prove |
|---|---|---|
| the repository zip | a file existed with those contents at that commit | ⚠⚠ **that anything is ABSENT. Positive record only** |
| ⚠ **no `SNAPSHOT_MANIFEST.txt`** | — | **whether the tree was DIRTY. It is a `git archive`** |
| `CLOSEOUT-2026-08-21.md` | what happened over two days | that any of it is still true |
| `PREWORK-2026-08-22.md` | what to do first | ⚠ intent, not state |
| **Phase 1's report** | ⚠⚠ **the two numbers that price everything** | — |
| `docs/README.md` | ⚠⚠ **IT GOVERNS** | that a cited number still means what the citing sentence assumed |

## §2 — Your grounding block. REPORT it before planning anything

1. **The snapshot hash**, and say out loud: *"I am working from a hand-carried snapshot at ‹hash›. It
   cannot prove anything is absent, and without a manifest I cannot say whether the tree was dirty."*
2. **Highest `### D-` and `### F-` from the HEADERS.**
3. ⚠⚠ **BOTH invariants WITH THEIR KEYS.** Last: **160 defined | 15 reserved | 175 cited | 0
   dangling**, amendment invariant clean. ⚠ **`defined` keys on `^### `; `cited` is a SET of distinct
   numbers, not a count of citations.** **`F-050` is RESERVED — do not take it.**
4. **Phase 1's two numbers in your own words.**
5. ⚠ **Ask Code what landed since the snapshot, BEFORE ruling on anything.**

## §3 — ⚠⚠ Six rules, every one bought in the last three days

1. ⚠⚠ **NEVER ASSERT ABSENCE, AND NEVER FROM A DOCUMENT *ABOUT* AN ENTRY.** *The outgoing Planner
   twice reasoned about `D-075` from a proposal about `D-075`, and asserted `geom_proxy` unbuilt in
   three documents while `F-017` records it as run id=5.*
2. ⚠⚠ **READ THE RIGHT LINK OF A SUPERSESSION CHAIN.** *It raised a stale-status alarm from a
   superseded copy it had itself asked to be preserved.*
3. ⚠ **CHECK THE RESOURCE CLASS.** *Four refutations this week — extraction assumed GPU; `preflight`
   assumed wired; "well inside the ceiling" at 0.03 GiB free; `F-042` assumed rental.*
4. ⚠⚠ **PRE-REGISTER THE BRANCHES YOU EXPECT AND THE SENTENCE THAT PERMITS A THIRD.** *Twice this
   week a two-branch pre-registration met a three-answer question.*
5. ⚠⚠ **PROPORTION IS ACCURACY.** *A documentation shortfall was written up as a confession and the
   owner asked whether he was committing a crime. He was not, and it was not close.*
6. ⚠ **DON'T GENERALISE A MEASURED FAILURE.** *"The tumour names cannot be matched up" turned four
   measured failures into a total impossibility — and it was false about seventeen of twenty.*

## §4 — ⚠⚠ The one thing that cannot be recovered if it happens in the wrong order

**IF THE POINT OF RENTING IS POWER, THE REFIT MUST BE RULED BEFORE ANY FOLD.**

⚠⚠ **`FC3` measured it: nothing pre-commits a refit at a larger `n`, anywhere.** **A refit chosen
after seeing the folds is post-hoc by construction.** **Ten minutes before; unsalvageable after.**

## §5 — Where the project is, in seven lines

**2,690 census folds live and searchable; 56 cohort targets scored at run 2.** ⚠ **No census row is
scored, by `D-089`.** **The clinical layer ships on two edges; HPA attribution is five-for-five.**
**The licence is RESOLVED — `v22` = BY-SA 3.0.** ⚠ **Seventeen of twenty tumour names join; thirteen
are blocked by nothing but a fetch.** ⚠⚠ **`P-001`'s comparator arm is POOLED, and its §4 measurement
returned UNDERPOWERED at n = 4.** **Tranche 5's 776 rows are held by `D-091` r2 — rental spend —
alone.**

## §6 — How this project works

**The log leads the code.** ⚠ *A commit message naming a decision does not discharge it.*
**Every claim names how it is known (`D-016`).** ⚠ **Every absence is a category with a cause.**
⚠⚠ **Two paths to one quantity, compared ON THE NUMBERS.**
**Corrections are recorded, never patched away — including yours.** ⚠ *Planner errors go in as
Planner findings; the catch rate runs both ways and that is the signal that matters.*
**Production writes require the owner at the keyboard.** ⚠ **Never infer flag syntax.**
⚠ **A permission denial is stop-and-report.**
**Artifacts carry an AUTHORED-SHA256 with its RANGE, inside the file, above a range excluding the
line carrying it.** ⚠⚠ **Select by HASH, never by filename — that has failed in both directions.**
⚠⚠ **A stopping condition that REMOVES the option beats one that forbids it.** *It cannot happen*
rather than *we checked*.

## §7 — ⚠ What to say first

**Report §2's five items. Then state §4 — the refit ruling — in one sentence, and STOP.**

⚠⚠ **The owner asked for rental. Give him rental.** **Two days of unplanned work were all valuable
and none of it was what was asked.**
