# ORDERS — Code — REISSUE: find the holes, name their kind, and plug the ones that can be plugged

**AUTHORED-SHA256** (range: **first `## §` header → EOF**, anchored to line starts, **SECOND
occurrence of the marker**) = `01949c9872e46bc0e4b52e64230d9a67f7725ce6ef12c5c7c81f839aa3c1d8ec`
**bytes** = `7712`

> ⚠ **DOWNLOAD AND COMMIT. DO NOT RETYPE.** Landing header **above** the marker, outside the range.
> ⚠⚠ **SUPERSEDES `ORDERS-Code-tumour-crosswalk.md` (`39c16d1a…`). DO NOT RUN THAT ONE.** **Its §5
> said *do not touch the copy until `VA` reports*, and that was wrong** — §1 explains why.
>
> ⚠⚠ **READING AND MEASUREMENT. NOTHING IS INGESTED, NO TABLE IS CREATED, NO PAGE IS BUILT.**
> **`D-093` is void if code precedes it; reading documentation is analysis.**
> ⚠ Grounding `main @ ad1a8b7`. **No GPU, no rental, no fold, no fit.**

---

## §0 — Owner ruling (2026-08-21)

> ⚠⚠ ***"We can just state that we don't have that data. And where we don't, we may want to seek it
> out. Find holes and then plug holes. Where we do have data, we should use it."***

**Three instructions, and the previous order only carried the first.** ⚠ **It inventoried the holes
and gave no route out of any of them.**

---

## §1 — ⚠⚠ Task WA — CORRECT THE COPY NOW. It does not wait on anything

**The live copy says *"the tumour names cannot be matched up."*** ⚠⚠ **`D-093` amendment 6 measured
FOUR failures of TWENTY.** **The sentence generalises four into a total impossibility.**

⚠ **The Planner said hold the copy until `VA` reports. That was wrong: the problem is not that the
copy is PREMATURE, it is that it is FALSE, and replacing a false claim with a true one waits on
nothing.**

**Ship this shape today:**

> **Incidence and survival are not shown — we do not have that data.** Some tumour names in this
> atlas cannot be reliably matched to registry categories; how many is being measured.

⚠ **Wording is yours; the constraints are:** **no total-impossibility claim** · **no number until
`VA`** · ⚠⚠ **and the LICENCE sentence separated from the VOCABULARY sentence and demoted** — *one
redistributable source was withdrawn on licensing grounds; that is true of the withdrawn source only
and is not why the public registries are unattempted.*

⚠ **And it is ONE LINE on the protein card.** **~150 words explaining an absence, on 2,690 cards,
outweighs the tumour panel further down that HAS data** — *which is how a reader concludes the
protein has none.* **The explanation belongs on the crosswalk page, not on every card.**

## §2 — Task WB — the crosswalk, twenty rows, FOUR verdicts

**One row per HPA `Cancer` value. All twenty, including the four known failures.**

**Per row:** the **HPA string verbatim** · **what HPA documents its samples to be** (§3) · the
**candidate registry category or categories** · **the aggregation if any** · the **verdict** · and
⚠ **for every non-`mapped` verdict, WHICH KIND of hole** (§5).

| verdict | meaning |
|---|---|
| `mapped` | names agree, no aggregation |
| ⚠ `mapped_with_aggregation` | **registry categories summed — THE AGGREGATION IS STATED, never implicit** |
| ⚠⚠ `mapped_at_stated_granularity` | **HPA's category is narrower than its name; both sides named for what they CONTAIN** |
| ⚠⚠ `refused` | **no counterpart — with its cause, and the cause is the useful part** |

⚠ **Two verdicts is not enough — `skin` proves the third exists, and collapsing it into either
neighbour is the failure mode.** ⚠ **`uncertain` is permitted and required where true. Do NOT round
toward `mapped`.** ⚠⚠ **Report the distribution AND the rows** — *a count of sixteen is not sixteen
rows.*

## §3 — Task WC — what HPA's samples actually ARE

⚠⚠ **HPA cannot be subdivided — it carries free-text names and no codes.** **So the question is not
*can we subdivide the join* but *what does HPA's category CONTAIN?***

**`WC1` — For each of the 20, report what HPA documents its tumour samples to be, from HPA's own
site.** ⚠ **If HPA's *"skin cancer"* cores are predominantly melanoma, the join is melanoma↔melanoma
— clean and honest, and `mapped_at_stated_granularity` rather than a workaround.**
**`WC2` — ⚠ Where HPA does not document it: `hpa_composition_undocumented`, a category with a cause.**

## §4 — ⚠⚠ Task WD — the skin question, READ not reasoned

**The Planner asserted SEER does not COLLECT basal-cell and squamous-cell skin cancer.** ⚠⚠ **THAT IS
GENERAL KNOWLEDGE, NOT MEASUREMENT.** **`D-093` amendment 1 exists because a licence was recalled
rather than read, and the Planner did the same thing three exchanges later.**

**`WD1` — Read SEER's own documentation on what its skin category covers and excludes. Quote it, URL
and date read.**
**`WD2` — ⚠⚠ If the exclusion is real, the finding is not *"the name is odd"* — it is that ***the
most common cancers in humans are systematically uncounted by the primary US registry***: a
COLLECTION hole, and the useful kind.**
**`WD3` — ⚠ Corroboration already on our surface: `LAMP1`'s `normal_tissues` carries *"skin 1"* and
*"skin 2"* as separate `High` entries.**

## §5 — ⚠⚠ Task WE — WHICH KIND of hole, because only one kind is a finding

| kind | what it is | useful? |
|---|---|---|
| ⚠⚠ **COLLECTION** | **the registry does not COUNT this population** | ⚠⚠ **YES — a real epidemiological gap** |
| ⚠ **JOIN** | **HPA's free text carries no code** | **NO — a task, not a finding** |
| ⚠ **UNMEASURED** | nobody has checked | **NEITHER, and must not be reported as either** |

⚠⚠ **Reporting a JOIN hole as a RESEARCH hole would be the exact over-claim `P-004` argues against in
someone else's work.** **Per row, say which.**

**`WE1` — ⚠⚠ AND THE ONE THAT REACHES THE RESEARCH QUESTION: for any HPA tumour type with no burden
counterpart, we hold tumour staining for a population whose SIZE AND MORTALITY NOBODY KNOWS.**
**Report how many of the 20 are in that state, and how many cohort/census proteins carry IHC in
them.** ⚠ **A target can look attractive in an indication with no denominator, and an ADC case cannot
be made without one.** **Measure it; the Planner frames it.**

## §6 — ⚠⚠ PLUG THE HOLES. The half the previous order was missing

**A route out of every verdict, stated so the crosswalk is an unblocking artifact rather than an
inventory of excuses.**

**`WF1` — ⚠ `mapped` and `mapped_with_aggregation` rows are READY.** **SEER is US Government, public
domain, and `D-093` amendment 6 already established *"credit the National Cancer Institute."***
⚠⚠ **Nothing blocks those rows but a fetch — REPORT THAT PLAINLY, because the current copy implies
otherwise.**
⚠ **But SEER has NEVER been through `D-093` decision 6's FIVE questions, and no supplier enters the
schema without them.** **Report what decision 6 would need; do not answer it here.**

**`WF2` — ⚠⚠ A JOIN hole is a TASK, and it is an afternoon.** **HPA has no codes; ICD-O has a
controlled vocabulary; a human maps twenty names to it once.** **Report which rows a hand-mapping
would plug, and what it would take.** ⚠ **Do not do the mapping under this order** — *it is a
decision about a controlled vocabulary and it wants its own entry.*

**`WF3` — ⚠⚠ A COLLECTION hole is a SEEK, not a dead end.** **SEER is US-only. GLOBOCAN/IARC counts
different populations under different rules** — ⚠ **a cancer uncounted by one registry may be counted
by another.** **Report which refused rows might have a counterpart in a second registry.**
⚠⚠ **Report only whether it is worth asking. Do not characterise IARC's terms** — *that is decision
6's question and it is the mistake this order already records twice.*

## §7 — ⚠ Not ordered

**No ingest, no registry data downloaded, no table, no schema, no page.** ⚠⚠ **Documentation only.**
**No hand-mapping. No supplier confirmation.** **No licence characterisation of SEER, IARC or anyone
else beyond what `D-093` amendment 6 already established.**
⚠ **If any question needs DATA rather than DOCUMENTATION, STOP AND REPORT.**

## §8 — Report

⚠ **`WA`'s corrected copy first — it ships independently of everything else.**
Then `WB`'s twenty rows with the verdict distribution · `WD1` quoted with URL and date · `WE`'s kinds
per row · ⚠⚠ **`WF1`'s count of rows that are ready and blocked only by a fetch** · branch and tip ·
both invariants with their keys · the gate without `.env`.
