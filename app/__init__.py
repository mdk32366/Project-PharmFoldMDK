"""The Fly serving tier (D-031) — FastAPI app + the four worker→Fly routes.

The first application code on Fly, and the first component the `search_path` seam
applies to as a service rather than a script (D-031). Nothing here is deployed until
the full suite passes.
"""
